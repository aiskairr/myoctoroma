# PWA Configuration Guide - Octō CRM

## ✅ Настройка PWA завершена!

Приложение теперь можно устанавливать как Progressive Web App (PWA) на любое устройство.

## 📱 Как установить приложение

### На Android:
1. Откройте сайт в Chrome
2. Нажмите на меню (три точки)
3. Выберите "Установить приложение" или "Add to Home screen"
4. Подтвердите установку

### На iOS (iPhone/iPad):
1. Откройте сайт в Safari
2. Нажмите кнопку "Поделиться" (квадрат со стрелкой вверх)
3. Прокрутите вниз и выберите "На экран «Домой»"
4. Нажмите "Добавить"

### На Desktop (Chrome/Edge):
1. Откройте сайт в браузере
2. В адресной строке справа появится иконка установки
3. Нажмите на иконку и выберите "Установить"

## 🎨 Генерация иконок для PWA

### Вариант 1: Использование HTML генератора (быстро)
1. Откройте файл `generate-pwa-icons.html` в браузере
2. Нажмите "Скачать все иконки"
3. Сохраните файлы `pwa-192x192.png` и `pwa-512x512.png` в папку `/public`
4. Обновите `vite.config.ts` - замените `.svg` на `.png` в секции icons

### Вариант 2: Онлайн инструменты (рекомендуется)
1. Зайдите на https://realfavicongenerator.net/
2. Загрузите файл `/public/PROM_logo_mid_blue.svg`
3. Настройте параметры:
   - Background color: `#0f172a` (темно-синий)
   - Theme color: `#0f172a`
4. Скачайте сгенерированные иконки
5. Поместите их в папку `/public`

### Вариант 3: Использование Sharp (профессиональный)
```bash
npm install -D sharp
node scripts/generate-icons.js
```

## 📦 Что уже настроено

### ✅ Файлы конфигурации:
- `vite.config.ts` - настроен плагин vite-plugin-pwa
- `index.html` - добавлены PWA мета-теги
- Временные SVG иконки созданы (для dev режима)

### ✅ Возможности PWA:
- 🔄 Автоматическое обновление service worker
- 📱 Работает в offline режиме (частично)
- 🎯 Кэширование API запросов (5 минут)
- 🎨 Splash screen с брендингом
- 📲 Устанавливается как нативное приложение
- 🌐 Работает на всех платформах

### ✅ Service Worker:
- Кэширует статические файлы (JS, CSS, HTML)
- Кэширует API запросы с NetworkFirst стратегией
- Автоматически обновляется при новой версии

## 🚀 Деплой

### Production Build:
```bash
npm run build
```

После сборки в папке `dist/` будут созданы:
- `manifest.webmanifest` - манифест PWA
- `sw.js` - service worker
- `workbox-*.js` - библиотека для кэширования

### Важно для деплоя:
1. Убедитесь что HTTPS включен (PWA требует HTTPS)
2. Манифест должен быть доступен по адресу `/manifest.webmanifest`
3. Service Worker должен быть доступен в корне сайта
4. Иконки должны быть в формате PNG для максимальной совместимости

## 🔧 Настройки PWA (vite.config.ts)

```typescript
VitePWA({
  registerType: 'autoUpdate',  // Автообновление
  manifest: {
    name: 'Octō CRM',
    short_name: 'Octō CRM',
    theme_color: '#0f172a',
    background_color: '#0f172a',
    display: 'standalone',
    orientation: 'portrait',
    // ... icons
  },
  workbox: {
    runtimeCaching: [
      // Кэширование API запросов
    ]
  }
})
```

## 📝 Следующие шаги

1. **Конвертируйте иконки в PNG** (см. Вариант 1 или 2 выше)
2. **Обновите vite.config.ts** - измените расширения иконок на `.png`
3. **Пересоберите проект**: `npm run build`
4. **Задеплойте** на сервер с HTTPS
5. **Протестируйте** установку на разных устройствах

## 🧪 Тестирование

### В браузере:
1. Откройте DevTools
2. Перейдите в Application → Manifest
3. Проверьте что манифест загружен корректно
4. Проверьте Service Worker в разделе Service Workers

### Lighthouse:
```bash
npm install -g lighthouse
lighthouse https://your-domain.com --view
```

Проверьте раздел "Progressive Web App" - должно быть 100 баллов.

## 📞 Поддержка

Если возникли вопросы:
- Проверьте консоль браузера на ошибки
- Убедитесь что HTTPS включен
- Проверьте что все файлы доступны
- Очистите кэш браузера и попробуйте снова

## 🎉 Готово!

Ваше приложение теперь поддерживает PWA и может быть установлено на любое устройство!
