# Форматы дат в API

## Стандарты форматов дат в системе

### 📅 scheduleDate - Дата расписания
**Формат:** `YYYY-MM-DD` (ISO date only)
**Примеры:** 
- `"2025-06-10T00:00:00.000Z"`
- `"2025-10-06T00:00:00.000Z"`

**Используется в:**
- `/api/tasks` - поле `scheduleDate`
- Календарные компоненты
- Фильтрация записей по дате

### 🕐 Временные метки (Timestamps)
**Формат:** `YYYY-MM-DDTHH:MM:SS.sssZ` (ISO 8601 с миллисекундами)
**Примеры:**
- `"2025-10-05T21:16:41.330Z"`
- `"2025-10-05T21:21:44.273Z"`

**Используется в:**
- `createdAt` - время создания записи
- `updatedAt` - время последнего обновления
- `firstSeenAt` - время первого появления клиента
- `lastActiveAt` - время последней активности клиента

### 🌍 Timezone
Все даты приходят в UTC (Z-суффикс) и должны быть конвертированы в локальную временную зону при отображении.

### 📝 Конвертация дат

#### Для scheduleDate:
```typescript
// Из API в локальную дату
const localDate = new Date(scheduleDate); // "2025-06-10T00:00:00.000Z" -> Date object

// Для отправки на API  
const apiDate = new Date(localDate.getTime() - (localDate.getTimezoneOffset() * 60000))
  .toISOString().split('T')[0]; // -> "2025-06-10"
```

#### Для timestamps:
```typescript
// Из API в локальную дату для отображения
const localDateTime = new Date(timestamp); // "2025-10-05T21:16:41.330Z" -> Date object
const formatted = format(localDateTime, "dd.MM.yyyy HH:mm"); // -> "05.10.2025 21:16"
```

### 🚨 Важные моменты
1. **scheduleDate** всегда содержит только дату без учета времени
2. **Timestamps** содержат полную информацию о дате и времени
3. Все даты в UTC, конвертируются в локальное время при отображении
4. При фильтрации по дате используется формат `YYYY-MM-DD`

### 📍 Места использования в коде

#### scheduleDate (YYYY-MM-DDTHH:MM:SS.sssZ):
- `src/hooks/use-task.ts` - функция `formatDate()` для отображения
- `src/hooks/use-tasks.ts` - интерфейс `TaskFromAPI.scheduleDate`
- `src/pages/Calendar/components/time-schedule.tsx` - фильтрация и создание записей
- `src/pages/DailyCalendar.tsx` - интерфейсы и конвертация дат
- `src/components/DailyCashReport.tsx` - фильтрация записей по дате

#### Timestamps (createdAt, updatedAt, firstSeenAt, lastActiveAt):
- `src/hooks/use-tasks.ts` - интерфейсы `TaskFromAPI` и `client`
- `src/pages/DailyCalendar.tsx` - интерфейс `Task.createdAt`
- Отображение времени создания/обновления записей
- Клиентская аналитика (первое появление, последняя активность)

#### Форматирование для API:
- **Отправка на сервер:** всегда `YYYY-MM-DD` для scheduleDate
- **Получение с сервера:** всегда полный ISO формат с временем
- **Отображение пользователю:** `DD.MM.YYYY` через `date-fns`
