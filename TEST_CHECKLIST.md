# ✅ Чеклист тестирования двух бэкендов

## Предварительная проверка

### 1. Проверка файлов конфигурации

- [ ] Файл `.env` существует и содержит оба URL
- [ ] `VITE_BACKEND_URL` указывает на Primary Backend
- [ ] `VITE_SECONDARY_BACKEND_URL` указывает на Secondary Backend
- [ ] `src/API/http.ts` обновлен
- [ ] `src/services/task-parser.ts` обновлен

**Команда для проверки:**
```bash
echo "=== .env файл ===" && cat .env && echo "" && \
echo "=== Первые строки http.ts ===" && head -15 src/API/http.ts
```

---

## Тестирование в браузере

### 2. Запуск приложения

- [ ] `npm run dev` запускается без ошибок
- [ ] Приложение открывается в браузере
- [ ] Нет ошибок в консоли при загрузке
- [ ] Нет TypeScript ошибок

**Команды:**
```bash
npm run dev
```

### 3. Проверка переменных окружения

Откройте консоль браузера (F12) и выполните:

```javascript
console.log('Primary:', import.meta.env.VITE_BACKEND_URL);
console.log('Secondary:', import.meta.env.VITE_SECONDARY_BACKEND_URL);
```

**Ожидаемый результат:**
- [ ] Primary: `https://lesser-felicdad-promconsulting-79f07228.koyeb.app`
- [ ] Secondary: `https://octobackend.com/api/main/`

---

## Тестирование API запросов

### 4. Тест Primary Backend

**В консоли браузера:**
```javascript
// Тест 1: Простой GET запрос
fetch('https://lesser-felicdad-promconsulting-79f07228.koyeb.app/api/tasks?branchId=1&sortBy=scheduleDate&sortOrder=asc&userRole=superadmin', {
  credentials: 'include'
})
.then(r => r.json())
.then(d => console.log('✅ Primary Backend:', d))
.catch(e => console.error('❌ Ошибка Primary:', e));
```

**Проверки:**
- [ ] Запрос выполнен успешно (статус 200)
- [ ] Получены данные от Primary Backend
- [ ] Нет CORS ошибок

### 5. Тест Secondary Backend

**В консоли браузера:**
```javascript
// Тест 2: GET запрос к Secondary Backend
fetch('https://octobackend.com/api/main//api/tasks?branchId=1&sortBy=scheduleDate&sortOrder=asc&userRole=superadmin', {
  credentials: 'include'
})
.then(r => r.json())
.then(d => console.log('✅ Secondary Backend:', d))
.catch(e => console.error('❌ Ошибка Secondary:', e));
```

**Проверки:**
- [ ] Запрос выполнен успешно (статус 200)
- [ ] Получены данные от Secondary Backend
- [ ] Нет CORS ошибок

### 6. Тест Helper функций

Создайте тестовый файл или выполните в компоненте:

```typescript
import { apiGetJson, createApiUrl } from '@/API/http';

// Тест createApiUrl
console.log('Primary URL:', createApiUrl('/api/tasks'));
console.log('Secondary URL:', createApiUrl('/api/tasks', true));

// Тест apiGetJson
apiGetJson('/api/tasks')
  .then(data => console.log('✅ apiGetJson Primary:', data))
  .catch(err => console.error('❌ Ошибка:', err));

apiGetJson('/api/tasks', true)
  .then(data => console.log('✅ apiGetJson Secondary:', data))
  .catch(err => console.error('❌ Ошибка:', err));
```

**Проверки:**
- [ ] createApiUrl генерирует правильные URL
- [ ] apiGetJson работает с Primary Backend
- [ ] apiGetJson работает с Secondary Backend (параметр true)

### 7. Тест TaskParserService

```typescript
import { taskParserService } from '@/services/task-parser';

// Проверка статуса
console.log('Статус парсера:', taskParserService.getStatus());

// Тест ручного запроса к Primary
taskParserService.manualFetch()
  .then(result => console.log('✅ Primary TaskParser:', result))
  .catch(err => console.error('❌ Ошибка:', err));

// Тест ручного запроса к Secondary
taskParserService.manualFetch({}, true)
  .then(result => console.log('✅ Secondary TaskParser:', result))
  .catch(err => console.error('❌ Ошибка:', err));
```

**Проверки:**
- [ ] manualFetch работает с Primary Backend
- [ ] manualFetch работает с Secondary Backend
- [ ] Возвращается правильная структура данных

---

## Network Tab проверки

### 8. Проверка запросов в DevTools

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Network**
3. Очистите логи (кнопка 🚫)
4. Выполните действие в приложении (например, откройте список задач)

**Проверки:**
- [ ] Запросы отображаются во вкладке Network
- [ ] URL запросов начинаются с Primary Backend URL
- [ ] Статус код: 200 (успех) или другой ожидаемый
- [ ] Response содержит правильные данные
- [ ] Headers содержат `credentials: include`

---

## Функциональное тестирование

### 9. Тест основных функций приложения

#### Задачи
- [ ] Список задач загружается
- [ ] Можно открыть детали задачи
- [ ] Можно создать новую задачу
- [ ] Можно обновить задачу
- [ ] Можно удалить задачу

#### Мастера
- [ ] Список мастеров загружается
- [ ] Можно добавить нового мастера
- [ ] Можно редактировать мастера
- [ ] Можно удалить мастера

#### Календарь
- [ ] Календарь отображается
- [ ] Задачи отображаются на календаре
- [ ] Можно создать задачу через календарь

#### Услуги
- [ ] Список услуг загружается
- [ ] Можно добавить новую услугу
- [ ] Можно редактировать услугу

---

## Тестирование переключения между бэкендами

### 10. Тест переключения через .env

**Шаг 1:** Измените `.env` файл:
```env
# Поменяйте местами URL
VITE_BACKEND_URL=https://octobackend.com/api/main/
VITE_SECONDARY_BACKEND_URL=https://lesser-felicdad-promconsulting-79f07228.koyeb.app
```

**Шаг 2:** Перезапустите dev сервер:
```bash
# Ctrl+C для остановки
npm run dev
```

**Шаг 3:** Проверьте в консоли браузера:
```javascript
console.log('Primary:', import.meta.env.VITE_BACKEND_URL);
// Должен показать: scattered-ermentrude...
```

**Проверки:**
- [ ] URL поменялись местами
- [ ] Запросы теперь идут на другой бэкенд
- [ ] Приложение работает корректно

**Шаг 4:** Верните обратно:
```env
VITE_BACKEND_URL=https://lesser-felicdad-promconsulting-79f07228.koyeb.app
VITE_SECONDARY_BACKEND_URL=https://octobackend.com/api/main/
```

---

## Стресс-тестирование

### 11. Параллельные запросы

```javascript
// Тест параллельных запросов к обоим бэкендам
Promise.all([
  fetch('https://lesser-felicdad-promconsulting-79f07228.koyeb.app/api/tasks?branchId=1', 
    { credentials: 'include' }).then(r => r.json()),
  fetch('https://octobackend.com/api/main//api/tasks?branchId=1', 
    { credentials: 'include' }).then(r => r.json())
])
.then(([primary, secondary]) => {
  console.log('✅ Primary:', primary);
  console.log('✅ Secondary:', secondary);
  console.log('✅ Оба бэкенда работают одновременно!');
})
.catch(err => console.error('❌ Ошибка:', err));
```

**Проверки:**
- [ ] Оба запроса выполняются одновременно
- [ ] Нет конфликтов между бэкендами
- [ ] Оба возвращают данные успешно

---

## Проверка обратной совместимости

### 12. Тест старого кода

Убедитесь, что старый код работает:

```typescript
// Старый способ (должен продолжать работать)
const url = `${import.meta.env.VITE_BACKEND_URL}/api/tasks`;
const response = await fetch(url, { credentials: 'include' });
const data = await response.json();
console.log('✅ Старый код работает:', data);
```

**Проверки:**
- [ ] Старый код работает без изменений
- [ ] `import.meta.env.VITE_BACKEND_URL` возвращает Primary URL
- [ ] Никаких breaking changes

---

## Production Build тест

### 13. Проверка production сборки

```bash
# Сборка для продакшена
npm run build

# Проверка на ошибки сборки
echo $?
# Должен вывести: 0 (успех)

# Локальный preview сборки
npm run preview
```

**Проверки:**
- [ ] Сборка завершается без ошибок
- [ ] Нет TypeScript ошибок
- [ ] Preview работает корректно
- [ ] Переменные окружения правильно подставлены

---

## Финальная проверка

### 14. Полный чеклист готовности

#### Конфигурация
- [ ] `.env` настроен правильно
- [ ] `src/API/http.ts` содержит оба API instance
- [ ] `src/services/task-parser.ts` использует createApiUrl
- [ ] Документация создана (5 файлов .md)

#### Функциональность
- [ ] Primary Backend работает
- [ ] Secondary Backend работает
- [ ] Helper функции работают
- [ ] TaskParserService работает с обоими бэкендами
- [ ] Обратная совместимость сохранена

#### Тестирование
- [ ] Unit тесты пройдены (если есть)
- [ ] Функциональные тесты пройдены
- [ ] Network запросы идут на правильные URL
- [ ] Нет CORS ошибок
- [ ] Нет console ошибок

#### Production
- [ ] Production сборка успешна
- [ ] Preview работает
- [ ] Готово к деплою

---

## 🎉 Результаты тестирования

### Пройдено: _____ / 14 секций

**Статус:**
- [ ] ✅ Все тесты пройдены - готово к использованию
- [ ] ⚠️ Частично пройдено - требуется доработка
- [ ] ❌ Тесты провалены - требуется исправление

**Заметки:**
```
_________________________________________________________________
_________________________________________________________________
_________________________________________________________________
```

---

**Дата тестирования:** _____________  
**Тестировщик:** _____________  
**Версия:** 1.0
