# Обновленная система высоты ячеек календаря

## 📏 Изменения в константах

### До изменений:
```typescript
const TIME_SLOT_HEIGHT = 32; // Не соответствовало h-6 (24px)
```

### После изменений:
```typescript
const TIME_SLOT_HEIGHT = 24; // Соответствует h-6 в Tailwind CSS
```

## 🔧 Обновленная логика размеров записей

### Категории размеров записей:
- **Очень маленькие** (`height <= 20px`): Только инициалы клиента
- **Маленькие** (`20px < height <= 30px`): Только имя клиента
- **Средние** (`30px < height <= 48px`): Имя клиента + услуга
- **Большие** (`height > 48px`): Полная информация + время

### Примеры для разных продолжительностей:
- **15 минут**: 1 слот × 24px = 22px (маленькая запись)
- **30 минут**: 2 слота × 24px = 46px (средняя запись)
- **45 минут**: 3 слота × 24px = 70px (большая запись)
- **60 минут**: 4 слота × 24px = 94px (большая запись)
- **90 минут**: 6 слотов × 24px = 142px (большая запись)

## 📋 Логика отображения содержимого

### Очень маленькие записи (≤20px):
```jsx
<div className="font-semibold truncate text-xs leading-none">
  {appointment.clientName.split(' ').map(name => name[0]).join('.')}
</div>
```
Отображает: "И.И." вместо "Иван Иванов"

### Маленькие записи (20-30px):
```jsx
<div className="font-semibold truncate text-xs leading-tight">
  {appointment.clientName}
</div>
```
Отображает: "Иван Иванов"

### Средние записи (30-48px):
```jsx
<div className="font-semibold truncate text-xs leading-tight">
  {appointment.clientName}
</div>
<div className="truncate text-xs opacity-70 leading-tight mt-0.5">
  {appointment.service}
</div>
```
Отображает: 
- "Иван Иванов"
- "VIP пакет"

### Большие записи (>48px):
```jsx
<div className="font-semibold truncate text-sm leading-tight">
  {appointment.clientName}
</div>
<div className="truncate text-xs opacity-70 leading-tight mt-0.5">
  {appointment.service}
</div>
<div className="text-xs opacity-60 mt-auto leading-tight">
  {appointment.startTime} - {appointment.endTime}
</div>
```
Отображает:
- "Иван Иванов"
- "VIP пакет"
- "09:45 - 11:15"

## 🔍 Логирование и отладка

### Логирование высоты записей:
```javascript
console.log(`📏 Appointment height calculation:`, {
  appointmentId: appointment.id,
  clientName: appointment.clientName,
  duration: appointment.duration,
  durationSlots: durationSlots, // Количество 15-минутных слотов
  TIME_SLOT_HEIGHT: TIME_SLOT_HEIGHT, // 24px
  calculatedHeight: height, // Итоговая высота
  startIndex: startIndex,
  startTime: appointment.startTime,
  endTime: appointment.endTime
});
```

### Логирование конвертации задач:
```javascript
console.log(`📋 Converted appointment:`, {
  id: appointment.id,
  clientName: appointment.clientName,
  startTime: appointment.startTime,
  endTime: appointment.endTime,
  duration: appointment.duration,
  employeeId: appointment.employeeId
});
```

## ✨ Преимущества изменений

1. **Консистентность**: TIME_SLOT_HEIGHT теперь точно соответствует высоте ячеек в CSS
2. **Адаптивность**: Записи автоматически адаптируются к своей высоте
3. **Читаемость**: Даже очень короткие записи остаются информативными
4. **Отладка**: Подробное логирование для диагностики проблем

## 🎯 Результат для примера данных

### Запись "VIP пакет" (90 минут):
- Продолжительность: 90 минут
- Слотов: Math.ceil(90/15) = 6 слотов
- Высота: 6 × 24px - 2px = 142px
- Категория: Большая запись
- Отображается: Имя + услуга + время

### Запись "Арома релакс" (120 минут):
- Продолжительность: 120 минут
- Слотов: Math.ceil(120/15) = 8 слотов
- Высота: 8 × 24px - 2px = 190px
- Категория: Большая запись
- Отображается: Полная информация

Теперь календарь корректно отображает записи с правильными пропорциями и адаптивным содержимым!
