# Инструкция по очистке данных авторизации

## Проблема
После логина роль пользователя сбрасывается на `client` или становится `undefined`.

## Решение
Я исправил проблему, теперь данные пользователя сохраняются в `localStorage` (более надежно) в дополнение к cookies.

## Что было исправлено:

1. **Убрал создание дефолтного пользователя с ролью 'client'**
   - Раньше при отсутствии данных создавался пользователь с ролью `client`
   - Теперь происходит разлогинивание

2. **Исправил парсинг ответа от `/user/auth`**
   - Поддержка двух вариантов структуры: `result.token` и `result.user.token`

3. **Добавил localStorage как основное хранилище**
   - Данные теперь сохраняются в `localStorage.user_data`
   - Cookies используются как резервное хранилище

4. **Добавил валидацию данных пользователя**
   - Проверяется наличие обязательных полей (`role`, `email`)

## Инструкция для тестирования:

### Шаг 1: Очистите все данные авторизации

Откройте консоль браузера (F12) и выполните:

```javascript
// Полная очистка всех данных авторизации
localStorage.clear();
document.cookie.split(";").forEach(c => { 
  document.cookie = c.replace(/^ +/, "").replace(/=.*/, "=;expires=" + new Date().toUTCString() + ";path=/"); 
});
console.log('✅ Все данные очищены. Обновите страницу.');
```

### Шаг 2: Перезагрузите страницу

Нажмите `Ctrl+R` (или `Cmd+R` на Mac)

### Шаг 3: Залогиньтесь заново

Войдите как owner используя ваши credentials:
- Email: `owner@gmail.com`
- Password: `^6A64N&Z`

### Шаг 4: Проверьте логи в консоли

После логина вы должны увидеть:

```
✅ User data saved to localStorage
✅ User data saved to cookies
✓ localStorage - role: owner
✓ localStorage - id: 2
✓ localStorage - email: owner@gmail.com
🎉 Login successful with role: owner
```

### Шаг 5: Перезагрузите страницу еще раз

После перезагрузки проверьте что роль сохранилась:

```
✅ User authenticated with role: owner
```

## Если проблема все еще есть

Проверьте в консоли браузера:

```javascript
// Проверка сохраненных данных
console.log('localStorage.user_data:', localStorage.getItem('user_data'));
console.log('localStorage.auth_token:', localStorage.getItem('auth_token'));
console.log('cookies.user:', document.cookie);
```

Все должно быть заполнено корректными данными.
