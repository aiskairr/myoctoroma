# Тестирование входа сотрудников (Staff Login Testing)

## Что было исправлено

Исправлена проблема с извлечением токена из staff API. Теперь система правильно обрабатывает формат ответа:
```json
{
  "data": {
    "token": "...",  // ← Токен теперь извлекается отсюда
    "user": {...}
  }
}
```

## Как протестировать

### 1. Очистите старые данные (опционально)
```javascript
// В DevTools Console
localStorage.clear();
```

### 2. Войдите как сотрудник

Используйте учетные данные сотрудника (staff).

### 3. Проверьте логи в Console

Должны увидеть последовательность:
```
Admin login response status: 401
Admin login failed, trying staffAuthorization/login...
Staff login response status: 200
✅ Token and user data received from backend
💾 Access token saved to localStorage
💾 User type detected and saved: staff
🎉 Login successful with role: manager
⏳ Waiting 2 seconds before redirect...
🔄 Redirecting manager to /dashboard
```

### 4. Проверьте localStorage

```javascript
// В DevTools Console
localStorage.getItem('user_type')     // Должно быть: 'staff'
localStorage.getItem('auth_token')    // Должен быть JWT токен
localStorage.getItem('user_data')     // Должны быть данные пользователя
```

### 5. Проверьте редирект

После успешного входа вы должны автоматически попасть на страницу `/dashboard`.

### 6. Проверьте последующие API запросы

Все API запросы должны использовать правильные эндпоинты:
- Logout: `POST /staffAuthorization/logout`
- Refresh: `POST /staffAuthorization/refresh`

## Проблемы?

### Если остались на странице логина:

1. Откройте DevTools Console
2. Найдите строку с `result.data?.token:`
3. Если написано `MISSING` - значит API вернул токен в другом формате

### Если видите 401 ошибки:

1. Проверьте что `localStorage.user_type` = `'staff'`
2. Проверьте что API запросы идут на правильный endpoint
3. Проверьте логи в Console для деталей

## Связанные документы

- `docs/STAFF_AUTH_INTEGRATION.md` - полное описание интеграции
- `docs/STAFF_LOGIN_FIX.md` - описание исправления
- `docs/ADMIN_AUTH_ENDPOINTS.md` - документация admin endpoints

## Тестовые сценарии

### ✅ Успешный вход
1. Email/password сотрудника
2. Redirect на /dashboard
3. localStorage.user_type = 'staff'

### ✅ Выход
1. Нажать кнопку выхода
2. Должен отправиться `POST /staffAuthorization/logout`
3. Redirect на /login

### ✅ Refresh токена
1. Дождаться истечения access token (или симулировать 401)
2. Должен отправиться `POST /staffAuthorization/refresh`
3. Получить новый access token
4. Повторить оригинальный запрос

### ✅ Проверка роли
Сотрудники с ролью 'manager' должны видеть Dashboard с соответствующими правами.
