# Конструктор системного промпта

**Дата:** 18 ноября 2025 г.  
**Статус:** ✅ Завершено

## Проблема
Ранее пользователи могли редактировать весь системный промпт, включая критически важную логику работы бота. Это приводило к:
- Случайному удалению важных инструкций
- Нарушению работы системы распознавания намерений
- Некорректным ответам бота клиентам
- Потере защиты от галлюцинаций ИИ

## Решение
Создан **конструктор системного промпта** с разделением на:

### 1. ✏️ Редактируемая часть (Кастомная роль)
Пользователь может редактировать только описание роли бота:
```
Ты - профессиональный администратор салона красоты/массажа.
```

**Примеры кастомных ролей:**
- "Ты - администратор барбершопа 'Стиль'"
- "Ты - менеджер спа-салона премиум класса"
- "Ты - профессиональный мастер маникюра салона 'Красотка'"

### 2. 🔒 Защищенная часть (Системная логика)
Автоматически добавляется к кастомной роли и содержит:
- 🎯 Принцип работы с Bot#1
- 🧠 Интеллектуальный контекст из БД
- ⚠️ Критические запреты (не выдумывать данные)
- 📌 Примеры правильной работы
- 🚨 Правила про создание записей
- 🚫 Запреты на выдумывание графиков
- 📋 Логика работы с услугами
- 💬 Стиль общения

## Архитектура

### Функции в Settings.tsx

#### `extractCustomRole(fullPrompt: string): string`
Извлекает кастомную роль из существующего полного промпта.
- Парсит строки до "🎯 ПРИНЦИП РАБОТЫ:"
- Возвращает только первую часть (роль)
- Дефолт: "Ты - профессиональный администратор салона красоты/массажа."

#### `generateFullPrompt(role: string): string`
Генерирует полный промпт для сохранения в БД.
- Принимает кастомную роль
- Добавляет `FIXED_PROMPT_TEMPLATE`
- Возвращает полный промпт для ChatGPT

#### `FIXED_PROMPT_TEMPLATE`
Константа с защищенной частью промпта (270+ строк).

### State Management
```typescript
const [customRole, setCustomRole] = useState("");
```

Хранит только редактируемую часть. При сохранении генерируется полный промпт.

## UI/UX

### Секции интерфейса

1. **📝 Описание роли бота** (Редактируемое поле)
   - Textarea на 3 строки
   - Зеленая рамка при фокусе
   - Плейсхолдер с примером
   - Бейдж "Редактируемое поле"

2. **🔒 Системная логика** (Защищена)
   - Read-only блок с прокруткой
   - Показывает содержимое `FIXED_PROMPT_TEMPLATE`
   - Бейдж "Автоматическая часть"
   - Пояснение о защите

3. **👁️ Предпросмотр полного промпта**
   - Синий блок с рамкой
   - Показывает итоговый промпт (роль + логика)
   - Обновляется при изменении роли
   - Прокрутка для длинного текста

4. **💾 Кнопка сохранения**
   - Disabled если роль пустая
   - Индикатор загрузки при сохранении
   - Зеленый цвет (emerald-600)

## Переводы

### Русский (ru)
```typescript
'settings.system_prompt_title': 'Конструктор системного промпта',
'settings.system_prompt_description': 'Настройте роль и описание вашего бота. Системная логика защищена и добавляется автоматически.',
```

### Кыргызский (ky)
```typescript
'settings.system_prompt_title': 'Системалык буйруктун конструктору',
'settings.system_prompt_description': 'Ботуңуздун ролун жана сүрөттөмөсүн жөндөңүз. Системалык логика корголгон жана автоматтык түрдө кошулат.',
```

### Английский (en)
```typescript
'settings.system_prompt_title': 'System Prompt Constructor',
'settings.system_prompt_description': 'Configure your bot role and description. System logic is protected and added automatically.',
```

## Технические детали

### Обратная совместимость
При загрузке существующего промпта:
- `extractCustomRole()` извлекает только кастомную часть
- Если промпт пустой → дефолтная роль
- Старые промпты парсятся корректно

### Сохранение
При нажатии "Сохранить":
1. Валидация: `customRole.trim()` не пустой
2. Генерация: `generateFullPrompt(customRole)`
3. PUT запрос: `/api/branches/${branchId}`
4. Обновление: `refetchBranches()`

### API
```typescript
PUT /api/branches/:id
Body: { systemPrompt: string }
Headers: { Authorization: Bearer <token> }
```

## Преимущества

### Для пользователей
✅ Простота — редактируют только описание бизнеса  
✅ Безопасность — нельзя сломать критическую логику  
✅ Понятность — видят, что защищено и почему  
✅ Гибкость — могут описать свой бренд  

### Для системы
✅ Стабильность — логика работы неизменна  
✅ Предсказуемость — всегда правильные инструкции  
✅ Масштабируемость — легко обновлять логику глобально  
✅ Безопасность — защита от человеческого фактора  

## Примеры использования

### Пример 1: Барбершоп
**Кастомная роль:**
```
Ты - администратор барбершопа "Men's Style". 
Мы специализируемся на мужских стрижках и уходе за бородой.
```

**Полный промпт:**
```
Ты - администратор барбершопа "Men's Style". 
Мы специализируемся на мужских стрижках и уходе за бородой.

🎯 ПРИНЦИП РАБОТЫ:
[... системная логика ...]
```

### Пример 2: Спа-салон
**Кастомная роль:**
```
Ты - менеджер премиум спа-салона "Релакс". 
У нас 10 видов массажа и VIP-кабинеты.
```

**Полный промпт:**
```
Ты - менеджер премиум спа-салона "Релакс". 
У нас 10 видов массажа и VIP-кабинеты.

🎯 ПРИНЦИП РАБОТЫ:
[... системная логика ...]
```

## Тестирование

### Проверено
✅ Создание нового промпта  
✅ Редактирование существующего  
✅ Парсинг старых промптов  
✅ Сохранение и обновление  
✅ Переводы (3 языка)  
✅ Валидация (пустое поле)  
✅ Предпросмотр обновляется  
✅ TypeScript: нет ошибок  

## Файлы изменены

1. **src/pages/Settings.tsx** (основные изменения)
   - Добавлен `FIXED_PROMPT_TEMPLATE`
   - Функции `extractCustomRole()` и `generateFullPrompt()`
   - State `customRole` вместо `systemPrompt`
   - Новый UI с 3 секциями

2. **src/contexts/LocaleContext.tsx**
   - Обновлены переводы `settings.system_prompt_title`
   - Обновлены переводы `settings.system_prompt_description`
   - Для всех 3 языков (ru, ky, en)

## Будущие улучшения

### Возможные доработки
- 💡 Шаблоны ролей (готовые примеры для разных бизнесов)
- 💡 История изменений промпта
- 💡 A/B тестирование разных ролей
- 💡 Аналитика эффективности промпта
- 💡 Экспорт/импорт промптов между филиалами

## Заметки разработчика

### Критические места
⚠️ Не изменять `FIXED_PROMPT_TEMPLATE` без тщательного тестирования  
⚠️ При обновлении логики — обновить версию в документации  
⚠️ Всегда тестировать парсинг старых промптов  

### Поддержка
При обновлении системной логики:
1. Изменить `FIXED_PROMPT_TEMPLATE`
2. Протестировать с разными ролями
3. Проверить генерацию полного промпта
4. Обновить версию в этом документе

---

**Версия системной логики:** v1.0  
**Последнее обновление:** 18.11.2025
