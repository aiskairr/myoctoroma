# Мобильные шторки (Bottom Sheets) для календаря

## Дата: 27 ноября 2025

## Обзор

Реализованы мобильные шторки (bottom sheets) вместо центральных диалогов для создания/редактирования записей и оплаты в календаре.

## Что изменилось

### ❌ Было: Центральный диалог
```
┌─────────────────┐
│                 │
│   ┌───────┐     │
│   │Dialog │     │  ← Диалог по центру экрана
│   └───────┘     │
│                 │
└─────────────────┘
```

### ✅ Стало: Шторка снизу
```
┌─────────────────┐
│                 │
│                 │  ← Остальной экран затемнён
│                 │
├─────────────────┤
│   ─────         │  ← Ручка для свайпа
│                 │
│   Содержимое    │  ← Шторка выезжает снизу
│                 │     max-h: 75vh
└─────────────────┘
```

## Компонент MobileDialog

### Основные характеристики

**Файл:** `src/components/ui/mobile-dialog.tsx`

#### 1. Позиционирование
```typescript
// Фиксация снизу экрана
className="fixed bottom-0 left-0 right-0 z-50 w-full"
```

#### 2. Внешний вид
```typescript
// Скругление только сверху (как у шторки)
"rounded-t-3xl"

// Тень и бордер
"border-t bg-background shadow-lg"
```

#### 3. Ограничение высоты
```typescript
// Максимум 75% высоты экрана
"max-h-[75vh] flex flex-col"
```

#### 4. Анимация
```typescript
// Выезжает снизу при открытии
"data-[state=open]:slide-in-from-bottom"
"data-[state=closed]:slide-out-to-bottom"
"duration-300"
```

#### 5. Характерная ручка
```tsx
{/* Серая полоска сверху для свайпа */}
<div className="mx-auto mt-2 mb-2 h-1.5 w-12 rounded-full bg-gray-300" />
```

### Структура компонента

```tsx
<MobileDialogContent>
  {/* Ручка для свайпа (автоматически) */}
  <div className="h-1.5 w-12 rounded-full bg-gray-300" />
  
  {/* Заголовок (не скроллится) */}
  <MobileDialogHeader>
    <MobileDialogTitle>Заголовок</MobileDialogTitle>
  </MobileDialogHeader>
  
  {/* Прокручиваемый контент */}
  <MobileDialogScrollContent>
    {/* Форма, список и т.д. */}
  </MobileDialogScrollContent>
  
  {/* Кнопки (не скроллятся) */}
  <MobileDialogFooter>
    <Button>Отмена</Button>
    <Button>Сохранить</Button>
  </MobileDialogFooter>
  
  {/* Кнопка закрытия (автоматически) */}
</MobileDialogContent>
```

## Технические детали

### 1. Flexbox Layout

```
┌─────────────────────────┐
│  MobileDialogContent    │ max-h-[75vh], flex flex-col
│  ┌───────────────────┐  │
│  │ Ручка (handle)    │  │ фиксирована
│  ├───────────────────┤  │
│  │ Header            │  │ flex-shrink-0, border-b
│  ├───────────────────┤  │
│  │                   │  │
│  │ ScrollContent     │  │ flex-1, overflow-y-auto
│  │ (прокрутка)       │  │ scroll-smooth
│  │                   │  │
│  ├───────────────────┤  │
│  │ Footer            │  │ flex-shrink-0, border-t
│  │                   │  │ pb-safe (для iPhone)
│  └───────────────────┘  │
└─────────────────────────┘
```

### 2. Кастомные анимации

**Добавлено в tailwind.config.js:**

```javascript
keyframes: {
  "slide-in-from-bottom": {
    from: { transform: "translateY(100%)" }, // Начинаем ниже экрана
    to: { transform: "translateY(0)" },      // Выезжаем на место
  },
  "slide-out-to-bottom": {
    from: { transform: "translateY(0)" },    // С текущей позиции
    to: { transform: "translateY(100%)" },   // Уезжаем вниз
  },
},
animation: {
  "slide-in-from-bottom": "slide-in-from-bottom 0.3s ease-out",
  "slide-out-to-bottom": "slide-out-to-bottom 0.3s ease-in",
}
```

### 3. Адаптивные стили

#### MobileDialogHeader
```typescript
className="flex flex-col space-y-1 text-left px-4 pt-2 pb-3 flex-shrink-0 border-b"
```
- Компактные отступы
- Бордер снизу для разделения
- Не скроллится (flex-shrink-0)

#### MobileDialogFooter
```typescript
className={cn(
  "flex flex-col gap-2 px-4 pb-safe pt-3 flex-shrink-0 border-t",
  // Safe area для iPhone (выемка снизу)
  "pb-[max(1rem,env(safe-area-inset-bottom))]"
)}
```
- Вертикальный layout (flex-col)
- Учитывает safe area для iPhone
- Бордер сверху для разделения

#### MobileDialogScrollContent
```typescript
className={cn(
  "overflow-y-auto overflow-x-hidden flex-1 px-4 py-3",
  "scrollbar-thin scrollbar-thumb-gray-300",
  "scroll-smooth",                    // Плавная прокрутка
  "[overflow-scrolling:touch]",       // Инерция для iOS
)}
```

#### MobileDialogTitle
```typescript
className="text-base font-semibold"  // Компактный размер
```

### 4. Поддержка iPhone

**Safe Area Insets:**
```css
pb-[max(1rem,env(safe-area-inset-bottom))]
```
Автоматически добавляет отступ снизу для iPhone с выемкой/динамическим островом.

### 5. Ручка для свайпа

```tsx
<div className="mx-auto mt-2 mb-2 h-1.5 w-12 rounded-full bg-gray-300" />
```
- Серая полоска 48px × 6px
- Скруглённая
- По центру сверху шторки
- Визуальный индикатор что можно свайпнуть вниз

## Сравнение: Диалог vs Шторка

| Параметр | Центральный Диалог | Bottom Sheet |
|----------|-------------------|--------------|
| **Позиция** | По центру экрана | Снизу экрана |
| **Анимация** | Zoom + fade | Slide вверх/вниз |
| **Закрытие** | Крестик, ESC, backdrop | Крестик, свайп вниз, backdrop |
| **Мобильность** | 🟡 Стандартно | 🟢 Нативно для мобильных |
| **UX** | Desktop-ориентирован | Mobile-first |
| **Высота** | max-h-[90vh] | max-h-[75vh] |
| **Скругление** | Все углы | Только сверху |
| **Ручка** | ❌ Нет | ✅ Есть |
| **Safe Area** | ❌ Не учитывает | ✅ Учитывает iPhone |

## Преимущества Bottom Sheet

### 1. Нативный UX
✅ Знакомый паттерн для мобильных (iOS, Android)
✅ Интуитивное взаимодействие (свайп вниз)
✅ Визуально понятно что это модальное окно

### 2. Эргономика
✅ Контент ближе к большому пальцу
✅ Кнопки снизу - удобнее нажимать
✅ Меньше движений пальцем

### 3. Пространство
✅ Ручка + заголовок занимают меньше места
✅ 75vh вместо 90vh - не загораживает весь экран
✅ Видно контекст сверху (календарь)

### 4. Анимация
✅ Плавное выезжание снизу
✅ Естественное движение
✅ 300ms - оптимальная скорость

### 5. Адаптивность
✅ Safe area для iPhone
✅ Плавный скролл с инерцией
✅ Тонкий scrollbar

## Использование

### Desktop (> 768px)
```tsx
<Dialog>  {/* Обычный центральный диалог */}
  <DialogContent className="max-w-[800px]">
    {/* Контент */}
  </DialogContent>
</Dialog>
```

### Mobile (< 768px)
```tsx
<MobileDialog>  {/* Шторка снизу */}
  <MobileDialogContent>
    <MobileDialogHeader>
      <MobileDialogTitle>Заголовок</MobileDialogTitle>
    </MobileDialogHeader>
    
    <MobileDialogScrollContent>
      {/* Прокручиваемый контент */}
    </MobileDialogScrollContent>
    
    <MobileDialogFooter>
      <Button>Кнопки</Button>
    </MobileDialogFooter>
  </MobileDialogContent>
</MobileDialog>
```

### В task-dialog-btn.tsx
```typescript
const isMobile = useIsMobile();
const DialogWrapper = isMobile ? MobileDialog : Dialog;
// ... условный рендеринг
```

## Поведение

### Открытие
1. Backdrop появляется (fade-in)
2. Шторка выезжает снизу (slide-in-from-bottom)
3. Длительность: 300ms

### Закрытие
1. Шторка уезжает вниз (slide-out-to-bottom)
2. Backdrop исчезает (fade-out)
3. Длительность: 300ms

### Прокрутка
- Header и Footer зафиксированы
- Только MobileDialogScrollContent прокручивается
- Плавная прокрутка с инерцией (iOS)
- Тонкий scrollbar (или скрытый)

## Визуальные детали

### Ручка (Handle)
```
  ─────  ← 48px wide, 6px tall, серая
```
- Цвет: `bg-gray-300`
- Размер: `w-12 h-1.5`
- Форма: `rounded-full`
- Позиция: `mx-auto mt-2 mb-2`

### Скругление
- Только сверху: `rounded-t-3xl` (24px радиус)
- Снизу прямые углы (прилегает к экрану)

### Разделители
- Header: `border-b` (снизу)
- Footer: `border-t` (сверху)
- Создают визуальное разделение секций

### Кнопка закрытия
- Позиция: `absolute right-4 top-4`
- Размер иконки: `h-5 w-5`
- Больше чем в обычном диалоге (для удобства тапа)

## Тестирование

Необходимо проверить:
- ✅ Шторка выезжает снизу
- ✅ Высота не превышает 75vh
- ✅ Ручка видна сверху
- ✅ Header и Footer не скроллятся
- ✅ Контент прокручивается плавно
- ✅ Кнопки доступны (с учётом safe area)
- ✅ Анимация плавная (300ms)
- ✅ Свайп вниз закрывает (если реализован)
- ✅ На iPhone учитывается выемка снизу

## Результат

✅ **Проблема решена:**
- Мобильные диалоги теперь работают как шторки
- Выезжают снизу (нативный паттерн)
- Ограничены 75% высоты экрана
- Имеют характерную ручку для свайпа
- Учитывают safe area для iPhone
- Плавная анимация и прокрутка
- Удобный mobile-first UX
