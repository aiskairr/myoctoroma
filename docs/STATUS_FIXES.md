# Исправления статусов задач

## Проблема
API возвращает статусы в формате `in_progress` и `completed`, но компоненты использовали смешанные форматы с дефисами `in-progress` и русскими названиями.

## Исправления

### 1. StatusBadge.tsx
✅ Добавлен перевод статуса `in_progress` → "В процессе"
✅ Добавлены статусы `scheduled` и `cancelled`
✅ Обновлены иконки и цвета для всех статусов

### 2. task-dialog-btn.tsx  
✅ Исправлены SelectItem values с русских на английские:
- `"В процессе"` → `"in_progress"`
- `"Завершен"` → `"completed"`
- `"Записан"` → `"scheduled"`
- `"Отменен"` → `"cancelled"`

### 3. use-task.ts
✅ Исправлена функция `mapStatus()`:
- `'in-progress'` → `'in_progress'`

### 4. time-schedule.tsx
✅ Исправлены все цветовые схемы статусов:
- `'in-progress'` → `'in_progress'` в getOverlapColor
- Обновлены statusColors, statusLabels, statusColorsTooltip

### 5. advanced-schedule-migrated.tsx
✅ Исправлены TypeScript типы:
- `'in-progress'` → `'in_progress'` в интерфейсах

## Результат

Теперь все компоненты корректно:
- **Получают** статусы от API в формате `in_progress`, `completed`
- **Отображают** русские переводы для пользователей
- **Отправляют** обратно на API правильные английские статусы
- **Показывают** правильные цвета для каждого статуса

## Поддерживаемые статусы

| API статус | Отображение | Цвет | Иконка |
|------------|-------------|------|--------|
| `scheduled` | Записан | Желтый | Clock |
| `in_progress` | В процессе | Синий | Clock |
| `completed` | Завершен | Зеленый | Check |
| `cancelled` | Отменен | Красный | XCircle |

Все компоненты теперь работают с единой системой статусов! 🎉
