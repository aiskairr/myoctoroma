# Исправления проблем календаря и статусов

## Проблемы и решения

### 1. 🎯 Статусы не отображаются в карточке редактирования

**Проблема:** Статусы в форме редактирования показывались как пустые поля.

**Причина:** Функция `mapStatus` в `use-task.ts` переводила английские статусы API в русские для формы, но в форме использовались английские значения.

**Решение:**
```typescript
// В use-task.ts - убрали перевод, оставляем оригинальные статусы API
const mapStatus = (status: string) => {
    return status; // Возвращаем английский статус для корректной работы формы
};
```

### 2. 💰 Суммы в диалоговом окне оплаты показывают 0

**Проблема:** Функция `calculateTotalPrice()` использовала только `basePrice` (по умолчанию 0) и дополнительные услуги, игнорируя основную стоимость задачи.

**Решение:**
```typescript
const calculateTotalPrice = useCallback((basePrice: number = 0) => {
    // Если basePrice не передан, используем цену из формы или данных задачи
    const mainPrice = basePrice || parseFloat(watch('cost')) || taskData?.finalPrice || taskData?.servicePrice || 0;
    const additionalPrice = additionalServices.reduce((sum, service) => sum + service.price, 0);
    return mainPrice + additionalPrice;
}, [additionalServices, watch, taskData]);
```

### 3. 🎨 Цвета задач не меняются из-за неправильной обработки статусов

**Было исправлено ранее:** В `time-schedule.tsx` заменили все `'in-progress'` на `'in_progress'` для соответствия API.

### 4. 📌 Закрепление боковой панели времени при горизонтальном скролле

**Проблема:** При прокрутке календаря вправо/влево панель времени исчезала из виду.

**Решение:**
```css
/* Добавили sticky left-0 к панели времени */
<div className="w-20 flex-shrink-0 border-r border-gray-200 bg-gray-50 sticky left-0 z-30">
    <div className="h-16 border-b border-gray-200 flex items-center justify-center sticky top-0 z-40 bg-gray-50 shadow-sm">
```

## Результат

### ✅ Статусы теперь:
- Корректно отображаются в карточке редактирования  
- Показывают правильные цвета для задач
- Работают во всех компонентах календаря

### ✅ Суммы в диалоге оплаты:
- Показывают реальную стоимость услуги
- Учитывают дополнительные услуги
- Правильно рассчитывают скидки

### ✅ UI/UX улучшения:
- Панель времени закреплена при горизонтальном скролле
- Шапка остается видимой при вертикальном скролле
- Плавная навигация по календарю

## Техническая информация

### Измененные файлы:
1. `/src/hooks/use-task.ts` - убрали перевод статусов в mapStatus
2. `/src/pages/Calendar/components/task-dialog-btn.tsx` - исправили calculateTotalPrice
3. `/src/pages/Calendar/components/time-schedule.tsx` - добавили sticky left-0 для панели времени

### z-index слои:
- `z-30` - панель времени (sticky left)
- `z-40` - заголовок панели времени (sticky top + left)

Все проблемы решены! 🎉
