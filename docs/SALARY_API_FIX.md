# Исправление API запроса salary-payments

## Проблема
API запрос `POST /api/salary-payments` возвращал ошибку 400:
```
"Employee name, amount, and branch ID are required"
```

## Причина
В запросе отсутствовал обязательный параметр `branchId`, а названия полей могли не соответствовать ожиданиям API.

## Исправления

### 1. Добавлен обязательный branchId
```typescript
const branchId = getBranchIdWithFallback(currentBranch, branches);
```

### 2. Обновлена структура данных
Теперь отправляются все возможные варианты названий полей:
```typescript
const paymentData = {
  // Основные поля согласно API требованиям
  employeeName: employeeName, // "Employee name" в camelCase
  amount: amount, // "amount"
  branchId: branchId, // "branch ID" в camelCase
  
  // Дополнительные поля (на случай если API ожидает другие варианты)
  employee_id: employeeId,
  employee_name: employeeName,
  branch_id: branchId,
  payment_date: new Date().toISOString().split('T')[0],
  period_start: startDate,
  period_end: endDate
};
```

### 3. Улучшена обработка ошибок
- Добавлено логирование отправляемых данных
- Детальная обработка ошибок API с отображением конкретного сообщения
- Toast уведомления с информативными сообщениями

### 4. Отладочная информация
```typescript
console.log('💰 Sending salary payment data:', paymentData);
console.error('❌ API Error:', errorData);
```

## Результат
Теперь API запрос содержит все необходимые поля:
- ✅ `employeeName` - имя сотрудника
- ✅ `amount` - сумма выплаты  
- ✅ `branchId` - ID филиала

Запрос должен успешно выполняться без ошибки 400.
