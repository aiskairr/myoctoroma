# 📖 Инструкция по использованию новых локализованных компонентов

## 🎯 Обзор

На странице "Как пользоваться" (How To Use Page) добавлены два новых компонента с полной поддержкой трех языков:

1. **Gift Certificate Video Card** - видеокурс о подарочных сертификатах
2. **Contact Support Card** - карточка для связи с поддержкой

## 🌍 Поддерживаемые языки

- 🇷🇺 **Русский** (ru)
- 🇰🇬 **Кыргызский** (ky)  
- 🇬🇧 **Английский** (en)

## 🎮 Как использовать

### 1. Проверка локализации на странице

1. Откройте страницу "How To Use" (`/how-to-use`)
2. В правом верхнем углу найдите переключатель языков
3. Выберите нужный язык:
   - 🇷🇺 RU (Русский)
   - 🇰🇬 KY (Кыргызский)
   - 🇬🇧 EN (Английский)

### 2. Видео о подарочных сертификатах

**Элементы компонента:**
- **Заголовок:** Автоматически переводится на выбранный язык
- **Описание:** Автоматически переводится на выбранный язык
- **Особенности:** Две колонки с информацией, все локализовано
- **Длительность:** 8 минут (одинаково для всех языков)
- **Кнопка:** "Смотреть" / "Watch" (в зависимости от языка)

**YouTube видео:** https://youtu.be/FFjsGbOPScY

### 3. Контактная карточка

**Элементы компонента:**
- **Заголовок:** "Свяжитесь с нами" / "Бизге байланыш" / "Contact Us"
- **Описание:** "Есть вопросы?" / "Сурооңуз барби?" / "Have questions?"
- **Email кнопка:** info@promconsult.pro (одинаково для всех языков)
- **Телефон кнопка:** +996 600 00 19 78 (одинаково для всех языков)

## 🔧 Разработку: Добавление новых ключей

Если вам нужно добавить новые ключи локализации:

### Шаг 1: Добавьте ключ в LocaleContext.tsx

```typescript
// В русской секции (ru):
'how_to_use.new_key': 'Русский текст',

// В кыргызской секции (ky):
'how_to_use.new_key': 'Кыргызский текст',

// В английской секции (en):
'how_to_use.new_key': 'English text',
```

### Шаг 2: Используйте в компоненте

```typescript
import { useLocale } from "@/contexts/LocaleContext";

export function MyComponent() {
  const { t } = useLocale();
  
  return <div>{t('how_to_use.new_key')}</div>;
}
```

## 📝 Примеры кода

### Пример 1: Использование простого ключа

```typescript
// In HowToUsePage.tsx
const { t } = useLocale();

<h2>{t('how_to_use.gift_certificate_video_title')}</h2>
// Выведет в зависимости от языка:
// РУ: "Подарочные Сертификаты и Абонименты"
// КY: "Белекке арналган Сертификаттар жана Абонименттер"
// EN: "Gift Certificates and Subscriptions"
```

### Пример 2: Использование с переменными

```typescript
const { t } = useLocale();

// Если ключ содержит переменную: '{duration} minutes'
<p>{t('duration', { duration: '8' })}</p>
// Выведет: "8 minutes"
```

### Пример 3: Условная локализация

```typescript
const { t, locale } = useLocale();

if (locale === 'ru') {
  console.log('Пользователь использует русский язык');
}

// Выведет текст в зависимости от текущего языка
<h3>{t('how_to_use.contact_title')}</h3>
```

## 🔍 Отладка

### Если текст не переводится

1. **Проверьте ключ:**
   ```typescript
   // Неправильно:
   t('how_to_use.gift_ceritificate_video_title') // опечатка
   
   // Правильно:
   t('how_to_use.gift_certificate_video_title')
   ```

2. **Проверьте языковые настройки:**
   - Откройте DevTools (F12)
   - В консоли введите: `localStorage.getItem('booking-locale')`
   - Должно вывести: "ru", "ky" или "en"

3. **Проверьте кэш браузера:**
   - Нажмите Ctrl+Shift+Delete и очистите кэш
   - Перезагрузите страницу (Ctrl+R или Cmd+R)

### Если видео не открывается

1. Проверьте интернет соединение
2. Убедитесь, что YouTube ID правильный: `FFjsGbOPScY`
3. Проверьте в консоли: открывается ли новая вкладка при клике на кнопку

### Если контакты не работают

**Email:**
```javascript
// Должно открыть email клиент
window.location.href = 'mailto:info@promconsult.pro';
```

**Телефон:**
```javascript
// Должно открыть приложение телефона (на мобильных)
window.location.href = 'tel:+996505963782';
```

## 📊 Структура локализации

```
src/contexts/LocaleContext.tsx
├── Русский (ru)
│   ├── how_to_use.gift_certificate_* (8 ключей)
│   └── how_to_use.contact_* (4 ключей)
├── Кыргызский (ky)
│   ├── how_to_use.gift_certificate_* (8 ключей)
│   └── how_to_use.contact_* (4 ключей)
└── Английский (en)
    ├── how_to_use.gift_certificate_* (8 ключей)
    └── how_to_use.contact_* (4 ключей)
```

## 🚀 Развертывание

### На локальной машине

```bash
# 1. Установите зависимости
npm install

# 2. Запустите dev сервер
npm run dev

# 3. Откройте страницу
# http://localhost:5173/how-to-use
```

### На production

```bash
# 1. Выполните build
npm run build

# 2. Проверьте результаты
ls -la dist/

# 3. Загрузите на сервер
# (используйте ваш метод развертывания)
```

## ✅ Чек-лист перед развертыванием

- [ ] Все три языка переводят текст корректно
- [ ] YouTube видео открывается в новой вкладке
- [ ] Email кнопка открывает почтовый клиент
- [ ] Телефон кнопка работает на мобильных устройствах
- [ ] Нет ошибок в консоли браузера
- [ ] Build проходит без ошибок
- [ ] Все модули трансформированы (3805)

## 📞 Контактная информация

Если нужна помощь или есть вопросы:

- 📧 **Email:** info@promconsult.pro
- 📱 **Телефон:** +996 600 00 19 78

## 📚 Дополнительные ресурсы

- [LocaleContext Documentation](./LOCALIZATION_KEYS_REFERENCE.md)
- [Localization Summary](./LOCALIZATION_SUMMARY.md)
- [Quick Status](./QUICK_LOCALIZATION_STATUS.md)

---

**Версия:** 1.0  
**Последнее обновление:** 30 октября 2025 г.  
**Статус:** ✅ Production Ready
