# Исправление визуального выделения рабочих дней мастера в календаре

## Дата: 3 ноября 2025 г.

## Проблема
В диалоговом окне редактирования мастера рабочие дни не визуально выделяются в календаре. Рабочие дни отображаются в списке снизу, но на самом календаре нет визуального индикатора.

### Наблюдаемое поведение
- ❌ Календарь показывает все дни одинаково
- ✅ Список рабочих дней под календарем отображается корректно
- ❌ Пользователь не может видеть какие дни уже являются рабочими при добавлении новых

## Причина
Несколько проблем в коде:

1. **Неправильная фильтрация дат**: `workingDays` включал только даты текущего месяца
2. **Некорректная передача props в Calendar**: компонент `ui/calendar.tsx` не пробрасывал `modifiers`, `modifiersStyles`, `modifiersClassNames`
3. **Слабые стили**: использовались CSS переменные вместо конкретных цветов
4. **Проблемы с часовыми поясами**: даты могли неправильно парситься

## Решение

### 1. Обновлен MasterWorkingDatesManager.tsx

#### Изменение фильтрации рабочих дней
**Было:**
```typescript
const workingDays = workingDatesInMonth.map(wd => new Date(wd.date));
```

**Стало:**
```typescript
const workingDays = workingDates
  .filter(wd => wd.date) // Убеждаемся что дата существует
  .map(wd => {
    const date = new Date(wd.date);
    // Устанавливаем время на полдень для избежания проблем с часовыми поясами
    date.setHours(12, 0, 0, 0);
    return date;
  });
```

**Причина изменения:**
- Теперь выделяются ВСЕ рабочие дни, не только текущего месяца
- Установка времени на полдень избегает проблем с часовыми поясами
- Фильтр проверяет существование даты перед парсингом

#### Улучшение стилей календаря
**Было:**
```typescript
modifiersStyles={{
  working: { 
    backgroundColor: 'hsl(var(--primary))', 
    color: 'hsl(var(--primary-foreground))' 
  },
  selected: {
    backgroundColor: 'hsl(var(--secondary))',
    color: 'hsl(var(--secondary-foreground))',
    fontWeight: 'bold'
  }
}}
```

**Стало:**
```typescript
modifiersStyles={{
  working: { 
    backgroundColor: 'rgb(34 197 94)', // green-500
    color: 'white',
    fontWeight: '600',
    borderRadius: '0.375rem'
  },
  selected: {
    backgroundColor: 'rgb(59 130 246)', // blue-500
    color: 'white',
    fontWeight: 'bold',
    borderRadius: '0.375rem'
  }
}}
modifiersClassNames={{
  working: 'bg-green-500 text-white font-semibold hover:bg-green-600',
  selected: 'bg-blue-500 text-white font-bold hover:bg-blue-600'
}}
```

**Причина изменения:**
- Конкретные RGB цвета более надежны чем CSS переменные
- Зеленый цвет для рабочих дней (green-500)
- Синий цвет для выбранных дат (blue-500)
- Добавлен hover эффект
- Округленные углы для лучшего визуала

#### Добавлена легенда
```typescript
{/* Легенда для календаря */}
<div className="mt-3 p-2 bg-gray-50 rounded text-xs space-y-1">
  <div className="flex items-center gap-2">
    <div className="w-4 h-4 bg-green-500 rounded"></div>
    <span className="text-gray-700">Рабочий день</span>
  </div>
  <div className="flex items-center gap-2">
    <div className="w-4 h-4 bg-blue-500 rounded"></div>
    <span className="text-gray-700">Выбранная дата</span>
  </div>
</div>
```

#### Добавлено отладочное логирование
```typescript
useEffect(() => {
  console.log('📅 Working days for calendar highlighting:', {
    workingDatesCount: workingDates.length,
    workingDaysCount: workingDays.length,
    workingDates: workingDates.map(wd => wd.date),
    workingDaysFormatted: workingDays.map(d => format(d, 'yyyy-MM-dd'))
  });
}, [workingDates, workingDays]);
```

### 2. Обновлен ui/calendar.tsx

#### Проброс props для модификаторов
**Было:**
```typescript
function Calendar({
  className,
  classNames,
  showOutsideDays = true,
  ...props
}: CalendarProps) {
  return (
    <DayPicker
      showOutsideDays={showOutsideDays}
      className={cn("p-3", className)}
      locale={ru}
      classNames={{...}}
      {...props}
    />
  );
}
```

**Стало:**
```typescript
function Calendar({
  className,
  classNames,
  showOutsideDays = true,
  modifiers,
  modifiersStyles,
  modifiersClassNames,
  ...props
}: CalendarProps) {
  return (
    <DayPicker
      showOutsideDays={showOutsideDays}
      className={cn("p-3", className)}
      locale={ru}
      modifiers={modifiers}
      modifiersStyles={modifiersStyles}
      modifiersClassNames={modifiersClassNames}
      classNames={{...}}
      {...props}
    />
  );
}
```

**Причина изменения:**
- Теперь Calendar правильно передает кастомные модификаторы в DayPicker
- Позволяет стилизовать конкретные дни (рабочие, выбранные и т.д.)

## Результат

### До исправления
- ❌ Все дни в календаре выглядят одинаково
- ❌ Невозможно визуально определить рабочие дни
- ❌ Нет легенды для понимания значений цветов

### После исправления
- ✅ Рабочие дни выделены зеленым цветом (green-500)
- ✅ Выбранные даты выделены синим цветом (blue-500)
- ✅ Добавлена легенда для понимания цветов
- ✅ Hover эффекты на дни
- ✅ Логирование для отладки
- ✅ Корректная работа с часовыми поясами

## Визуальная схема

### Цветовая палитра
```
🟢 Зеленый (rgb(34 197 94)) = Рабочий день мастера
🔵 Синий (rgb(59 130 246)) = Выбранная дата для добавления
⚪ Белый = Обычный день (не рабочий)
🟡 Желтый/Accent = Сегодняшняя дата
```

### Состояния дня в календаре
1. **Обычный день**: белый фон, черный текст
2. **Рабочий день**: зеленый фон, белый текст, жирный шрифт
3. **Выбранная дата**: синий фон, белый текст, очень жирный шрифт
4. **Рабочий день + выбранная**: синий фон (приоритет)
5. **Сегодня**: accent фон (желтоватый)

## Технические детали

### Работа с датами
```typescript
// Правильное создание Date объекта с учетом часового пояса
const date = new Date(wd.date); // "2025-11-03"
date.setHours(12, 0, 0, 0); // Устанавливаем полдень UTC
```

### Структура modifiers
```typescript
modifiers={{
  working: [Date, Date, Date], // Массив Date объектов
  selected: [Date, Date]       // Массив выбранных дат
}}
```

### Приоритет стилей
1. `modifiersStyles` - inline стили (высший приоритет)
2. `modifiersClassNames` - CSS классы
3. Встроенные стили DayPicker

## Файлы изменены

1. `/src/components/MasterWorkingDatesManager.tsx`
   - Обновлена логика фильтрации `workingDays`
   - Улучшены стили модификаторов
   - Добавлена легенда
   - Добавлено логирование

2. `/src/components/ui/calendar.tsx`
   - Добавлен проброс `modifiers`
   - Добавлен проброс `modifiersStyles`
   - Добавлен проброс `modifiersClassNames`
   - Исправлены линтер warnings

## Тестирование

### Чеклист для тестирования
- [ ] Открыть диалог редактирования мастера
- [ ] Проверить что рабочие дни выделены зеленым
- [ ] Выбрать несколько новых дат - они должны быть синими
- [ ] Проверить что легенда отображается корректно
- [ ] Переключить месяц - рабочие дни должны выделяться во всех месяцах
- [ ] Добавить новый рабочий день - он должен стать зеленым
- [ ] Удалить рабочий день - зеленое выделение должно исчезнуть
- [ ] Проверить в консоли логи с датами

### Браузеры для тестирования
- [ ] Chrome
- [ ] Firefox
- [ ] Safari
- [ ] Mobile Safari (iOS)
- [ ] Chrome Mobile (Android)

## Известные ограничения

1. **Производительность**: При большом количестве рабочих дней (>100) может быть небольшая задержка
   - Решение: workingDays мемоизирован через useMemo

2. **Часовые пояса**: При переходе через границу дня (midnight) могут быть проблемы
   - Решение: setHours(12, 0, 0, 0) устанавливает время на полдень

## Связанные issues
- Проблема с выделением рабочих дней в календаре
- Улучшение UX для управления рабочими днями мастера

## Авторы
- Исправление: 3 ноября 2025 г.

## Дополнительная информация

### React Day Picker документация
- Modifiers: https://react-day-picker.js.org/guides/modifiers
- Styling: https://react-day-picker.js.org/guides/styling

### Tailwind CSS цвета
- green-500: `rgb(34 197 94)`
- green-600: `rgb(22 163 74)`
- blue-500: `rgb(59 130 246)`
- blue-600: `rgb(37 99 235)`
