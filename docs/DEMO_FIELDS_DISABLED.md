# Временное отключение демо-полей на странице Настроек

**Дата:** 23 октября 2025  
**Автор:** Система автоматизации  
**Затронутые файлы:** 
- `src/components/BookingLinkCopy.tsx`
- `src/pages/Settings.tsx`

## Обзор изменений

Временно деактивированы и помечены как "Демо" следующие секции на странице Настроек:
1. Ссылка мессенджера для организации
2. Ссылка мессенджера для филиала
3. Настройки системного промпта
4. WhatsApp API конфигурация

## Визуальные изменения

### Общий дизайн деактивированных секций

Все деактивированные секции получили единообразный стиль:

```tsx
className="bg-gray-100 border-gray-300 opacity-60 relative"
```

**Бейдж "Демо":**
```tsx
<div className="absolute top-2 right-2 bg-gray-500 text-white text-xs px-2 py-1 rounded-md font-semibold">
  Демо
</div>
```

### 1. Ссылки мессенджера (BookingLinkCopy.tsx)

#### Ссылка мессенджера для организации

**Изменения:**
- ✅ Фон карточки: `bg-gray-100` (было: `bg-white/80`)
- ✅ Граница: `border-gray-300` (было: `border-purple-200`)
- ✅ Прозрачность: `opacity-60`
- ✅ Иконки: серые (`text-gray-500`)
- ✅ Input: отключён (`disabled`), серый фон
- ✅ Кнопки: все отключены (`disabled`)
- ✅ Бейдж "Демо" в правом верхнем углу

**До:**
```tsx
<div className="bg-white/80 backdrop-blur-sm rounded-xl p-4 border border-purple-200">
  <Input 
    className="bg-gradient-to-r from-purple-50 to-purple-100"
    onClick={() => copyToClipboard(...)}
  />
  <Button onClick={...} className="bg-gradient-to-r from-purple-500 to-indigo-600">
```

**После:**
```tsx
<div className="bg-gray-100 border-gray-300 opacity-60 relative">
  <div className="absolute top-2 right-2 bg-gray-500 text-white text-xs px-2 py-1 rounded-md font-semibold">
    Демо
  </div>
  <Input disabled className="bg-gray-200 border-gray-300 text-gray-500 cursor-not-allowed" />
  <Button disabled className="bg-gray-400 text-white cursor-not-allowed opacity-50">
```

#### Ссылка мессенджера для филиала

Аналогичные изменения:
- Серый фон и границы
- Отключены все интерактивные элементы
- Бейдж "Демо"

### 2. Настройки системного промпта (Settings.tsx)

**Изменения:**
- ✅ Card: серый фон с бейджем "Демо"
- ✅ CardTitle: `text-gray-600`
- ✅ Textarea: отключён, серый фон
- ✅ Label: `text-gray-600`
- ✅ Кнопки: обе отключены

**До:**
```tsx
<Card className="mb-6">
  <CardTitle>{t('settings.system_prompt_title')}</CardTitle>
  <Textarea 
    value={settings.systemPrompt}
    onChange={(e) => handleInputChange("systemPrompt", e.target.value)}
  />
  <Button onClick={resetSystemPrompt}>{t('settings.reset_to_default')}</Button>
  <Button onClick={() => handleSave("systemPrompt")}>{t('settings.save_prompt')}</Button>
```

**После:**
```tsx
<Card className="mb-6 bg-gray-100 border-gray-300 opacity-60 relative">
  <div className="absolute top-4 right-4 bg-gray-500 text-white text-xs px-2 py-1 rounded-md font-semibold z-10">
    Демо
  </div>
  <CardTitle className="text-gray-600">{t('settings.system_prompt_title')}</CardTitle>
  <Textarea 
    disabled
    className="bg-gray-200 border-gray-300 text-gray-500 cursor-not-allowed"
  />
  <Button disabled className="bg-gray-400 text-white cursor-not-allowed opacity-50">
```

### 3. WhatsApp API конфигурация (Settings.tsx)

**Изменения:**
- ✅ Card: серый фон с бейджем "Демо"
- ✅ Все 4 Input поля: отключены, серые
  - API URL
  - Media URL
  - Branch ID
  - API Token
- ✅ Кнопки "Сохранить" и "Тест соединения": отключены
- ✅ Информационный блок: серый фон

**До:**
```tsx
<Card>
  <CardTitle>{t('settings.whatsapp_api')}</CardTitle>
  <Input 
    value={whatsappConfig.apiUrl}
    onChange={(e) => handleWhatsappInputChange('apiUrl', e.target.value)}
  />
  <Button onClick={handleSaveWhatsappConfig}>
  <Button onClick={handleTestConnection}>
```

**После:**
```tsx
<Card className="bg-gray-100 border-gray-300 opacity-60 relative">
  <div className="absolute top-4 right-4 bg-gray-500 text-white text-xs px-2 py-1 rounded-md font-semibold z-10">
    Демо
  </div>
  <CardTitle className="text-gray-600">{t('settings.whatsapp_api')}</CardTitle>
  <Input 
    disabled
    className="bg-gray-200 border-gray-300 text-gray-500 cursor-not-allowed"
  />
  <Button disabled className="bg-gray-400 text-white cursor-not-allowed opacity-50">
```

## Функциональность

### Сохранённые функции
- ❌ Копирование ссылок
- ❌ Открытие ссылок
- ❌ Генерация QR-кодов
- ❌ Редактирование системного промпта
- ❌ Редактирование WhatsApp конфигурации
- ❌ Тестирование подключения

### Визуально доступные, но неактивные
- ✅ Отображение текущих значений полей
- ✅ Отображение плейсхолдеров
- ✅ Отображение структуры интерфейса

## Технические детали

### Цветовая палитра
- **Фон карточек:** `bg-gray-100`
- **Границы:** `border-gray-300`
- **Текст заголовков:** `text-gray-600`
- **Текст в полях:** `text-gray-500`
- **Фон полей ввода:** `bg-gray-200`
- **Бейдж "Демо":** `bg-gray-500 text-white`
- **Прозрачность:** `opacity-60`

### CSS классы для disabled элементов
```css
cursor-not-allowed
bg-gray-200
border-gray-300
text-gray-500
opacity-50
```

### Z-index для бейджа
```tsx
className="z-10"
```
Используется для того, чтобы бейдж "Демо" оставался поверх всех элементов карточки.

## Неиспользуемый код

После деактивации полей остались неиспользуемые функции (не критично):

**BookingLinkCopy.tsx:**
- `linksLoading` - переменная
- `Trash2` - иконка

**Settings.tsx:**
- `isTestingConnection` - состояние
- `handleInputChange` - функция
- `handleSave` - функция  
- `handleWhatsappInputChange` - функция
- `handleSaveWhatsappConfig` - функция
- `handleTestConnection` - функция
- `resetSystemPrompt` - функция

Эти элементы можно оставить для будущего восстановления функционала.

## Причина изменений

Временное отключение демо-функций для:
1. Предотвращения случайного изменения критических настроек
2. Обозначения функционала в разработке
3. Показа UI без активации функций

## Восстановление функционала

Для восстановления необходимо:
1. Удалить бейдж "Демо"
2. Убрать `disabled` атрибуты
3. Вернуть исходные цветовые схемы (purple/indigo)
4. Вернуть обработчики событий (onClick, onChange)
5. Убрать `opacity-60`
6. Вернуть градиентные фоны

## Совместимость

- ✅ Сборка проекта: успешна
- ✅ TypeScript: только warnings о неиспользуемых функциях
- ✅ Функционал остальных страниц: не затронут
- ✅ Локализация: полностью работает

## Результат

Пользователи видят:
- 📊 Структуру и содержимое демо-полей
- 🚫 Явное обозначение "Демо"
- 🔒 Невозможность редактирования
- 👁️ Визуальную индикацию неактивного состояния
