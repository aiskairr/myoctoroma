# 📲 Добавление поля "Номер телефона" для мастеров

## 🎯 Обзор

Добавлено новое поле `phoneNumber` в формы создания и редактирования мастеров для поддержки WhatsApp уведомлений о новых записях.

## 📋 Что было добавлено

### 1. Интерфейс Master
```typescript
interface Master {
  id: number;
  name: string;
  specialty?: string;
  description?: string;
  phoneNumber?: string; // ✅ НОВОЕ ПОЛЕ
  isActive: boolean;
  startWorkHour: string;
  endWorkHour: string;
  createdAt: string;
  photoUrl?: string;
  workingDates?: WorkingDate[];
}
```

### 2. Форма создания/редактирования мастера

**Добавлено в состояние формы:**
```typescript
const [formData, setFormData] = useState({
  name: master?.name || '',
  specialty: master?.specialty || '',
  description: master?.description || '',
  phoneNumber: master?.phoneNumber || '', // ✅ НОВОЕ
  isActive: master?.isActive ?? true,
  startWorkHour: master?.startWorkHour || '09:00',
  endWorkHour: master?.endWorkHour || '20:00',
});
```

**UI в форме (после поля "Специальность"):**
```tsx
<div className="grid grid-cols-4 items-center gap-4">
  <Label htmlFor="phoneNumber" className="col-span-1 text-sm font-medium text-gray-700">
    📲 {t('masters.phone_number')}
  </Label>
  <div className="col-span-3 space-y-2">
    <Input
      id="phoneNumber"
      name="phoneNumber"
      type="tel"
      value={formData.phoneNumber}
      onChange={handleChange}
      className="rounded-lg border-gray-300 focus:ring-indigo-500 focus:border-indigo-500"
      placeholder="+996701234567 или 0701234567"
    />
    <p className="text-xs text-gray-500">
      💬 Для WhatsApp уведомлений о новых записях (опционально)
    </p>
  </div>
</div>
```

### 3. Отображение в карточке мастера

**Добавлено после рабочего времени:**
```tsx
{master.phoneNumber && (
  <div className="flex items-center text-sm text-gray-600">
    <span className="mr-2">📲</span>
    <span className="font-medium">{master.phoneNumber}</span>
    <span className="ml-2 text-xs text-green-600">WhatsApp</span>
  </div>
)}
```

### 4. Локализация

**Русский (ru):**
```typescript
'masters.phone_number': 'Номер телефона'
```

**Кыргызский (ky):**
```typescript
'masters.phone_number': 'Телефон номери'
```

**Английский (en):**
```typescript
'masters.phone_number': 'Phone Number'
```

## 🔄 Интеграция с API

### Создание мастера
**POST** `/api/crm/masters/:branchId`

```json
{
  "name": "Иван Мастер",
  "specialty": "Массаж",
  "phoneNumber": "+996701234567", // ✅ Отправляется
  "description": "Опытный мастер",
  "startWorkHour": "09:00",
  "endWorkHour": "20:00",
  "isActive": true
}
```

### Редактирование мастера
**PUT** `/api/crm/masters/:id`

```json
{
  "name": "Иван Мастер",
  "phoneNumber": "+996701234567", // ✅ Обновляется
  "specialty": "Массаж VIP",
  "startWorkHour": "09:00",
  "endWorkHour": "21:00",
  "isActive": true
}
```

## 📱 Форматы номеров

Поле принимает два формата:
- **Международный:** `+996701234567`
- **Национальный:** `0701234567`

Backend должен нормализовать номер перед отправкой WhatsApp уведомления.

## ✨ Особенности

### Опциональное поле
- Поле **не обязательно** для заполнения
- Если не указано - мастер просто не получает WhatsApp уведомления
- Можно добавить/удалить номер в любой момент через редактирование

### Визуальные подсказки
- **Эмодзи 📲** - легко узнаваемая иконка телефона
- **Placeholder** - показывает примеры форматов
- **Подсказка** - объясняет назначение поля
- **Badge "WhatsApp"** - в карточке мастера показывает, что это WhatsApp номер

### Отображение
- В карточке показывается **только если номер указан**
- Рядом с номером - зеленый badge "WhatsApp" для понятности
- Консистентный дизайн с остальными элементами карточки

## 🧪 Тестовые сценарии

### Сценарий 1: Создание мастера с номером
1. Открыть "Добавить мастера"
2. Заполнить обязательные поля (имя)
3. Ввести номер: `+996701234567`
4. Нажать "Сохранить"
5. **Ожидание:** Мастер создан с номером

### Сценарий 2: Создание мастера без номера
1. Открыть "Добавить мастера"
2. Заполнить обязательные поля (имя)
3. Оставить поле "Номер телефона" пустым
4. Нажать "Сохранить"
5. **Ожидание:** Мастер создан без номера, уведомления отключены

### Сценарий 3: Добавление номера к существующему мастеру
1. Открыть редактирование мастера без номера
2. Ввести номер: `0701234567`
3. Нажать "Сохранить"
4. **Ожидание:** Номер сохранен, мастер начнет получать уведомления

### Сценарий 4: Удаление номера
1. Открыть редактирование мастера с номером
2. Очистить поле "Номер телефона"
3. Нажать "Сохранить"
4. **Ожидание:** Номер удален, уведомления отключены

### Сценарий 5: Отображение в карточке
1. Создать мастера с номером `+996701234567`
2. Посмотреть на карточку мастера
3. **Ожидание:** Видна строка "📲 +996701234567 WhatsApp"

## 📂 Измененные файлы

1. `/src/pages/Masters.tsx`
   - Добавлено `phoneNumber` в интерфейс `Master`
   - Добавлено поле в форму создания/редактирования
   - Добавлено отображение в карточке мастера

2. `/src/contexts/LocaleContext.tsx`
   - Добавлен перевод `masters.phone_number` для 3 языков

## 🔗 Связанная документация

- API документация мастер-уведомлений (предоставлена в запросе)
- `/docs/API_SPECIFICATION.md` - общая API спецификация

## 📅 Дата реализации
4 ноября 2025 г.
