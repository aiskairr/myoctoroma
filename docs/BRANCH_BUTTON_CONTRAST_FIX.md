# Улучшение контрастности кнопки филиалов

## Дата: 27 ноября 2025 г.

## Проблема

Кнопка выбора филиала имела белый текст на полупрозрачном светло-зелёном фоне, что создавало плохую читаемость и низкий контраст.

### До изменений:
```tsx
// Плохой контраст
className="... from-emerald-500/20 to-teal-500/20 ... text-white ..."
```

**Проблемы:**
- ❌ Белый текст на полупрозрачном светлом фоне
- ❌ Низкая контрастность (WCAG accessibility fail)
- ❌ Плохая читаемость, особенно при ярком освещении
- ❌ Не соответствует стандартам доступности

---

## Решение

Изменили цветовую схему на тёмный текст на ярком зелёном фоне для максимальной контрастности.

### После изменений:
```tsx
// Отличный контраст
className="... from-emerald-400 to-teal-400 ... text-slate-900 ..."
```

**Улучшения:**
- ✅ Тёмно-синий/чёрный текст (`text-slate-900`)
- ✅ Яркий зелёный фон (без прозрачности)
- ✅ Высокая контрастность (WCAG AAA)
- ✅ Отличная читаемость
- ✅ Профессиональный внешний вид

---

## Детальное сравнение

### Цвета фона

| Параметр | До | После |
|----------|-----|--------|
| **Базовый градиент** | `from-emerald-500/20 to-teal-500/20` | `from-emerald-400 to-teal-400` |
| **Прозрачность** | 20% (очень прозрачный) | 100% (полностью непрозрачный) |
| **Яркость** | Тусклая | Яркая |
| **Hover градиент** | `from-emerald-500/30 to-teal-500/30` | `from-emerald-500 to-teal-500` |

### Цвета текста

| Параметр | До | После |
|----------|-----|--------|
| **Базовый текст** | `text-white` (белый) | `text-slate-900` (тёмный) |
| **Hover текст** | `text-white` | `text-slate-900` (остаётся тёмным) |
| **Вес шрифта** | `font-medium` | `font-semibold` |
| **Контраст** | ⚠️ Низкий | ✅ Высокий |

### Рамка

| Параметр | До | После |
|----------|-----|--------|
| **Базовая рамка** | `border-emerald-500/50` | `border-emerald-500` |
| **Hover рамка** | `border-emerald-400` | `border-emerald-600` |
| **Видимость** | Слабая | Чёткая |

### Тень

| Параметр | До | После |
|----------|-----|--------|
| **Базовая тень** | `shadow-md` | `shadow-md` |
| **Hover тень** | `shadow-lg` | `shadow-xl` |
| **Эффект** | Средний | Более выраженный |

---

## Полный код изменений

### До:
```tsx
<Button 
  variant="outline" 
  size="sm"
  className="bg-gradient-to-r from-emerald-500/20 to-teal-500/20 border-emerald-500/50 hover:from-emerald-500/30 hover:to-teal-500/30 hover:border-emerald-400 text-white font-medium shadow-md hover:shadow-lg transition-all duration-200 gap-1.5 h-8 px-3"
>
  <Building2 className="h-4 w-4" />
  <span className="text-xs">{currentBranch?.branches || t('branch.title')}</span>
</Button>
```

### После:
```tsx
<Button 
  variant="outline" 
  size="sm"
  className="bg-gradient-to-r from-emerald-400 to-teal-400 border-emerald-500 hover:from-emerald-500 hover:to-teal-500 hover:border-emerald-600 text-slate-900 hover:text-slate-900 font-semibold shadow-md hover:shadow-xl transition-all duration-200 gap-1.5 h-8 px-3"
>
  <Building2 className="h-4 w-4" />
  <span className="text-xs">{currentBranch?.branches || t('branch.title')}</span>
</Button>
```

---

## Визуальное сравнение

### До:
```
┌────────────────────┐
│  🏢 Центральный   │  ← Белый на светло-зелёном
└────────────────────┘    (плохо читается)
  Прозрачный фон
```

### После:
```
┌────────────────────┐
│  🏢 Центральный   │  ← Тёмный на ярко-зелёном
└────────────────────┘    (отлично читается!)
  Яркий зелёный фон
```

---

## Доступность (Accessibility)

### Коэффициент контрастности WCAG

| Комбинация | До | После | Стандарт |
|------------|-----|--------|----------|
| **Обычный текст** | ~2.5:1 ❌ | ~9.2:1 ✅ | 4.5:1 (AA) |
| **Крупный текст** | ~2.5:1 ⚠️ | ~9.2:1 ✅ | 3:1 (AA) |
| **Уровень** | Fail | AAA | - |

**Результат:** Теперь кнопка соответствует стандарту WCAG 2.1 Level AAA! ✅

---

## Психология цвета

### Новая схема "Тёмный на зелёном":

✅ **Зелёный фон:**
- Ассоциируется с действием, подтверждением
- Привлекает внимание
- Означает "можно/доступно"

✅ **Тёмный текст:**
- Максимальная читаемость
- Профессионализм
- Серьёзность и надёжность

---

## Файлы изменены

- ✏️ `src/components/BranchSelector.tsx`

---

## Сборка

```bash
npm run build
# ✓ built in 9.41s
# No errors found
```

---

## Как проверить

1. Откройте мобильную версию (< 768px)
2. Посмотрите на кнопку филиала в правом верхнем углу
3. Проверьте:
   - ✅ Фон яркий зелёный (изумрудно-бирюзовый градиент)
   - ✅ Текст тёмный (почти чёрный)
   - ✅ Текст легко читается
   - ✅ При hover фон становится ещё ярче
   - ✅ Тень увеличивается при hover

---

## Технические детали

### Tailwind классы

**Фон:**
- `from-emerald-400` - Начало градиента (яркий изумрудный)
- `to-teal-400` - Конец градиента (яркий бирюзовый)
- Hover: `from-emerald-500 to-teal-500` (ещё ярче)

**Текст:**
- `text-slate-900` - Очень тёмный синевато-серый (почти чёрный)
- `hover:text-slate-900` - Остаётся тёмным при hover
- `font-semibold` - Полужирный для лучшей читаемости

**Рамка:**
- `border-emerald-500` - Средний изумрудный
- Hover: `border-emerald-600` - Тёмный изумрудный

**Эффекты:**
- `shadow-md` → `hover:shadow-xl` - Увеличение тени
- `transition-all duration-200` - Плавная анимация

---

## Результат

✅ **Отличная читаемость** - текст чётко виден при любом освещении  
✅ **Высокая контрастность** - соответствует WCAG AAA  
✅ **Привлекает внимание** - яркая кнопка выделяется  
✅ **Профессиональный вид** - современный дизайн  
✅ **Лучший UX** - пользователи сразу понимают, что это кнопка

Кнопка филиалов теперь яркая, контрастная и отлично читается! 🎨✨
