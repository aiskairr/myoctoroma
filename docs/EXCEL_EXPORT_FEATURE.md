# Экспорт отчетов в Excel (XLSX)

## Дата: 27 ноября 2025

## Обзор

Добавлена функциональность экспорта финансовых отчетов в формат Excel (.xlsx) для удобной работы с данными в электронных таблицах.

## Установленные зависимости

```bash
npm install xlsx
```

**Библиотека:** `xlsx` (SheetJS)
- Полная поддержка кириллицы
- Создание Excel файлов в браузере
- Настройка ширины колонок, стилей, метаданных

## Реализация

### src/pages/ReportPage.tsx

**Импорты:**
```typescript
import { FileSpreadsheet } from 'lucide-react';
import * as XLSX from 'xlsx';
```

**Функция exportToExcel:**

```typescript
const exportToExcel = () => {
  try {
    // Заголовки таблицы
    const headers = [
      'Дата',
      'Общая выручка',
      'Расходы',
      'Доход',
      'Касса',
      'Optima',
      'MBank',
      'MBusiness',
      'Demir',
      'Bakai',
      'OBank',
      'Инкассация',
      'Выплата з/п',
      'Заметки',
    ];

    // Данные из reports
    const data = reports.map(report => [
      new Date(report.date).toLocaleDateString('ru-RU'),
      Number(report.total_revenue) || 0,
      Number(report.petty_expenses) || 0,
      Number(report.total_income) || 0,
      Number(report.end_balance) || 0,
      Number(report.optima_payments) || 0,
      Number(report.mbank_payments) || 0,
      Number(report.mbusiness_payments) || 0,
      Number(report.demir_payments) || 0,
      Number(report.bakai_payments) || 0,
      Number(report.obank_payments) || 0,
      Number(report.cash_collection) || 0,
      Number(report.salary_payments) || 0,
      report.notes || '',
    ]);

    // Итоговая строка
    const totals = calculateTotals();
    data.push([
      'ИТОГО',
      totals.totalRevenue,
      totals.totalExpenses,
      totals.totalIncome,
      totals.totalCash,
      totals.totalOptima,
      totals.totalMBank,
      totals.totalMBusiness,
      totals.totalDemir,
      totals.totalBakai,
      totals.totalOBank,
      totals.totalCollection,
      totals.totalSalaryPayments,
      '',
    ]);

    // Создание worksheet
    const ws = XLSX.utils.aoa_to_sheet([headers, ...data]);

    // Настройка ширины колонок
    ws['!cols'] = [
      { wch: 12 },  // Дата
      { wch: 15 },  // Общая выручка
      { wch: 12 },  // Расходы
      { wch: 12 },  // Доход
      { wch: 12 },  // Касса
      { wch: 12 },  // Optima
      { wch: 12 },  // MBank
      { wch: 12 },  // MBusiness
      { wch: 12 },  // Demir
      { wch: 12 },  // Bakai
      { wch: 12 },  // OBank
      { wch: 15 },  // Инкассация
      { wch: 15 },  // Выплата з/п
      { wch: 30 },  // Заметки
    ];

    // Создание workbook
    const wb = XLSX.utils.book_new();
    XLSX.utils.book_append_sheet(wb, ws, 'Отчеты');

    // Метаданные
    wb.Props = {
      Title: `Отчеты за период ${startDate} - ${endDate}`,
      Subject: 'Финансовые отчеты',
      Author: 'ElitaRoma',
      CreatedDate: new Date(),
    };

    // Сохранение файла
    const fileName = `отчет_${startDate}_${endDate}.xlsx`;
    XLSX.writeFile(wb, fileName);

    toast({
      title: 'Excel экспортирован',
      description: `Файл ${fileName} успешно сохранен`,
    });
  } catch (error) {
    console.error('Ошибка при создании Excel:', error);
    toast({
      title: t('common.error'),
      description: 'Не удалось создать Excel',
      variant: 'destructive',
    });
  }
};
```

**UI - Две кнопки экспорта:**

```tsx
<div className="flex gap-2">
  <Button
    onClick={exportToExcel}
    variant="outline"
    size="sm"
    className="flex items-center gap-2 text-xs"
  >
    <FileSpreadsheet className="h-4 w-4" />
    Excel
  </Button>
  <Button
    onClick={exportToPDF}
    variant="outline"
    size="sm"
    className="flex items-center gap-2 text-xs"
  >
    <FileDown className="h-4 w-4" />
    PDF
  </Button>
</div>
```

## Структура Excel файла

### Лист "Отчеты"

**Колонки (14 штук):**
1. **Дата** - формат DD.MM.YYYY
2. **Общая выручка** - числовое значение
3. **Расходы** - числовое значение
4. **Доход** - числовое значение
5. **Касса** - числовое значение
6. **Optima** - числовое значение
7. **MBank** - числовое значение
8. **MBusiness** - числовое значение
9. **Demir** - числовое значение
10. **Bakai** - числовое значение
11. **OBank** - числовое значение
12. **Инкассация** - числовое значение
13. **Выплата з/п** - числовое значение
14. **Заметки** - текстовое значение

**Строки:**
- Заголовок (строка 1)
- Данные отчетов (строки 2...N)
- Итоговая строка "ИТОГО" (строка N+1)

### Ширина колонок:
- Стандартные данные: 12 символов
- Числовые с большими значениями: 15 символов
- Заметки: 30 символов (для длинного текста)

### Метаданные файла:
- **Title**: "Отчеты за период [дата] - [дата]"
- **Subject**: "Финансовые отчеты"
- **Author**: "ElitaRoma"
- **CreatedDate**: текущая дата создания

## Особенности

### 1. Поддержка кириллицы
✅ Полная поддержка русских и кыргызских символов
- Заголовки на кириллице
- Текст в заметках
- Слово "ИТОГО" в итоговой строке

### 2. Числовые данные
✅ Все суммы экспортируются как числа (Number)
- Excel распознает их как числа
- Можно применять формулы и расчеты
- Автоматическое форматирование

### 3. Даты
✅ Даты в русском формате: `30.10.2025`
- Экспортируются как текст для стабильности
- Можно преобразовать в формат даты Excel при необходимости

### 4. Итоговая строка
✅ Автоматически добавляется строка с суммами:
- Использует функцию `calculateTotals()`
- Содержит все агрегированные данные
- Четко обозначена словом "ИТОГО"

## Преимущества Excel экспорта

### Для пользователей:
1. **Редактирование** - можно изменять данные, добавлять комментарии
2. **Расчеты** - применять формулы Excel для анализа
3. **Фильтрация** - использовать фильтры и сортировку
4. **Графики** - создавать диаграммы и графики
5. **Интеграция** - импортировать в другие системы

### Технические:
1. **Малый размер файла** - текстовый формат с компрессией
2. **Быстрая генерация** - без рендеринга canvas
3. **Точность данных** - числа остаются числами, не изображениями
4. **Универсальность** - открывается в Excel, Google Sheets, LibreOffice

## Сравнение с PDF

| Параметр | Excel (XLSX) | PDF |
|----------|--------------|-----|
| **Редактирование** | ✅ Да | ❌ Нет |
| **Кириллица** | ✅ Отлично | ✅ Отлично |
| **Размер файла** | 🟢 Маленький | 🟡 Средний |
| **Скорость** | 🟢 Быстро | 🟡 Медленнее |
| **Формулы** | ✅ Да | ❌ Нет |
| **Визуализация** | 🟡 Базовая | 🟢 Отличная |
| **Печать** | 🟡 Нужна настройка | 🟢 Готов к печати |

## Имя файла

Формат: `отчет_[startDate]_[endDate].xlsx`

Примеры:
- `отчет_2025-09-30_2025-10-30.xlsx`
- `отчет_2025-10-01_2025-10-31.xlsx`

## Использование

1. Пользователь открывает страницу "Отчеты"
2. Выбирает период и филиал (опционально)
3. Нажимает кнопку **"Excel"**
4. Файл автоматически скачивается
5. Открывается в Excel/Google Sheets/LibreOffice

## Тестирование

Необходимо проверить:
- ✅ Файл создается и скачивается
- ✅ Кириллица отображается корректно
- ✅ Все 14 колонок присутствуют
- ✅ Числовые данные распознаются как числа
- ✅ Итоговая строка содержит правильные суммы
- ✅ Ширина колонок удобна для чтения
- ✅ Метаданные файла заполнены
- ✅ Заметки полностью видны

## Возможные улучшения

### Стилизация:
- [ ] Жирный шрифт для заголовков
- [ ] Фон для заголовков (серый)
- [ ] Фон для итоговой строки (светло-серый)
- [ ] Границы ячеек (grid)
- [ ] Выравнивание (числа справа, текст слева)

### Форматирование:
- [ ] Числовой формат с разделителями тысяч: `1 000`
- [ ] Формат валюты: `1 000 сом`
- [ ] Формат даты Excel (для расчетов)

### Дополнительные листы:
- [ ] Лист "Сводка" с общими итогами
- [ ] Лист "Графики" с визуализацией
- [ ] Лист "По филиалам" с разбивкой

## Результат

✅ **Функционал полностью реализован:**
- Экспорт всех данных в Excel
- Поддержка кириллицы
- Две кнопки экспорта: Excel и PDF
- Удобная работа с данными в таблицах
- Быстрая генерация файла
