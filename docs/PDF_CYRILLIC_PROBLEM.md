# Проблема с кириллицей в PDF отчетах

## Дата: 27 ноября 2025 г.

## Проблема

При экспорте отчетов в PDF кириллический текст (русский/кыргызский) отображается некорректно - вместо слов появляются крякозябры или пропуски. При этом числа отображаются нормально.

## Причина

**jsPDF по умолчанию не поддерживает кириллицу!**

Библиотека jsPDF использует стандартные шрифты PDF (Helvetica, Times, Courier), которые содержат только:
- Латинские символы (A-Z, a-z)
- Цифры (0-9)
- Базовые знаки пунктуации

❌ НЕТ поддержки:
- Кириллицы (А-Я, а-я)
- Специальных символов других языков
- Unicode символов

## Решения

### Вариант 1: Добавление кастомного шрифта (РЕКОМЕНДУЕТСЯ)

Нужно добавить шрифт с поддержкой кириллицы (например, Roboto, PT Sans, DejaVu Sans).

#### Шаги:

1. **Установить пакет для конвертации шрифтов:**

```bash
npm install --save-dev jspdf-customfonts
```

2. **Или использовать онлайн конвертер:**
   - Конвертируйте TTF шрифт в base64 на сайте: https://rawgit.com/MrRio/jsPDF/master/fontconverter/fontconverter.html
   - Скачайте файл .js со шрифтом

3. **Добавить шрифт в проект:**

```typescript
// src/utils/roboto-font.ts
import { jsPDF } from 'jspdf';

export const addRobotoFont = (doc: jsPDF) => {
  // Здесь будет base64 строка шрифта Roboto
  const robotoBase64 = "..."; // Очень длинная base64 строка
  
  doc.addFileToVFS("Roboto-Regular.ttf", robotoBase64);
  doc.addFont("Roboto-Regular.ttf", "Roboto", "normal");
  doc.setFont("Roboto");
  
  return doc;
};
```

4. **Использовать в ReportPage:**

```typescript
import { addRobotoFont } from '@/utils/roboto-font';

const exportToPDF = () => {
  const doc = new jsPDF('landscape');
  
  // Добавляем шрифт с кириллицей
  addRobotoFont(doc);
  
  // Теперь можно использовать кириллицу
  doc.text('Отчеты за период', 14, 15); // ✅ Работает!
  
  // ...
};
```

### Вариант 2: Использование html2canvas + jsPDF

Рендерим HTML таблицу как изображение и добавляем в PDF.

#### Установка:

```bash
npm install html2canvas
```

#### Код:

```typescript
import html2canvas from 'html2canvas';
import { jsPDF } from 'jspdf';

const exportToPDF = async () => {
  // Получаем HTML элемент с таблицей
  const element = document.getElementById('report-table');
  
  if (!element) return;
  
  // Конвертируем в canvas
  const canvas = await html2canvas(element, {
    scale: 2, // Качество
    useCORS: true,
    logging: false,
  });
  
  const imgData = canvas.toDataURL('image/png');
  const pdf = new jsPDF('landscape');
  
  const imgWidth = 280; // A4 landscape width
  const imgHeight = (canvas.height * imgWidth) / canvas.width;
  
  pdf.addImage(imgData, 'PNG', 10, 10, imgWidth, imgHeight);
  pdf.save(`отчет_${startDate}_${endDate}.pdf`);
};
```

**Преимущества:**
- ✅ Идеальное отображение - как на экране
- ✅ Поддержка всех шрифтов и стилей
- ✅ Не нужно добавлять кастомные шрифты

**Недостатки:**
- ❌ Нельзя выделить текст в PDF (это изображение)
- ❌ Больший размер файла
- ❌ Может быть размытым при печати

### Вариант 3: Использовать серверную генерацию PDF

Генерировать PDF на backend (Node.js/Python) с использованием библиотек, которые нативно поддерживают кириллицу:
- Puppeteer (Chrome Headless)
- wkhtmltopdf
- WeasyPrint (Python)

### Вариант 4: Временное решение (ТЕКУЩЕЕ)

Использовать транслитерацию для заголовков:

```typescript
doc.text('Otchety za period', 14, 15); // Вместо "Отчеты за период"
doc.text('Filial: ...', 14, 28); // Вместо "Филиал: ..."
```

**Недостатки:**
- ❌ Неудобно читать
- ❌ Теряется смысл
- ❌ Не профессионально

## Рекомендуемое решение

### Для быстрого исправления: Вариант 2 (html2canvas)

Это самое быстрое решение, которое даст идеальный результат без необходимости работать с шрифтами.

### Для долгосрочного решения: Вариант 1 (кастомный шрифт)

Это правильное решение, которое позволит:
- Выделять текст в PDF
- Меньший размер файлов
- Лучшее качество при печати

## Пример реализации Варианта 2 (html2canvas)

```typescript
// 1. Установка
npm install html2canvas

// 2. Импорт
import html2canvas from 'html2canvas';
import { jsPDF } from 'jspdf';

// 3. Добавить id к таблице
<div id="report-table-wrapper">
  <table id="report-table">
    {/* ... таблица ... */}
  </table>
</div>

// 4. Функция экспорта
const exportToPDF = async () => {
  const element = document.getElementById('report-table-wrapper');
  
  if (!element) {
    toast({
      title: 'Ошибка',
      description: 'Таблица не найдена',
      variant: 'destructive',
    });
    return;
  }

  try {
    // Показываем индикатор загрузки
    toast({
      title: 'Генерация PDF',
      description: 'Подождите, идет создание документа...',
    });

    // Создаем canvas из HTML
    const canvas = await html2canvas(element, {
      scale: 2,
      useCORS: true,
      logging: false,
      backgroundColor: '#ffffff',
    });

    // Создаем PDF
    const imgData = canvas.toDataURL('image/png');
    const pdf = new jsPDF('landscape', 'mm', 'a4');
    
    const pdfWidth = pdf.internal.pageSize.getWidth();
    const pdfHeight = pdf.internal.pageSize.getHeight();
    
    const imgWidth = pdfWidth - 20; // Отступы
    const imgHeight = (canvas.height * imgWidth) / canvas.width;
    
    let heightLeft = imgHeight;
    let position = 10;

    // Добавляем первую страницу
    pdf.addImage(imgData, 'PNG', 10, position, imgWidth, imgHeight);
    heightLeft -= pdfHeight;

    // Добавляем дополнительные страницы если нужно
    while (heightLeft > 0) {
      position = heightLeft - imgHeight + 10;
      pdf.addPage();
      pdf.addImage(imgData, 'PNG', 10, position, imgWidth, imgHeight);
      heightLeft -= pdfHeight;
    }

    // Сохраняем
    const fileName = `отчет_${startDate}_${endDate}.pdf`;
    pdf.save(fileName);

    toast({
      title: 'PDF экспортирован',
      description: `Файл ${fileName} успешно сохранен`,
    });
  } catch (error) {
    console.error('Ошибка при создании PDF:', error);
    toast({
      title: 'Ошибка',
      description: 'Не удалось создать PDF',
      variant: 'destructive',
    });
  }
};
```

## Сравнение вариантов

| Характеристика | html2canvas | Кастомный шрифт | Серверная генерация |
|----------------|-------------|-----------------|---------------------|
| Сложность | ⭐ Легко | ⭐⭐ Средне | ⭐⭐⭐ Сложно |
| Качество текста | ⭐⭐ Хорошо | ⭐⭐⭐ Отлично | ⭐⭐⭐ Отлично |
| Размер файла | ⭐⭐ Большой | ⭐⭐⭐ Маленький | ⭐⭐⭐ Маленький |
| Скорость | ⭐⭐ Средняя | ⭐⭐⭐ Быстро | ⭐ Медленно |
| Выделение текста | ❌ Нет | ✅ Да | ✅ Да |
| Настройка | Минимальная | Средняя | Высокая |

## Итог

**Рекомендую:** Вариант 2 (html2canvas) как самое быстрое и простое решение с хорошим результатом.

**Для production:** Рассмотреть Вариант 1 (кастомный шрифт) для лучшего качества и возможности работы с текстом.

---

**Статус:** 🔍 Проблема выявлена  
**Причина:** Отсутствие поддержки кириллицы в jsPDF  
**Решение:** Требуется добавление шрифта или использование html2canvas
