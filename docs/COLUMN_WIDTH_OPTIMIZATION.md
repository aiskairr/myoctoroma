# Оптимизация ширины колонок мастеров

## Изменения

Ширина колонок мастеров в календаре уменьшена на **20%** для лучшего размещения на экране.

## Обновленные параметры

### До изменения:
- **Минимальная ширина**: 250px
- **Дефолтная ширина**: 300px  
- **Максимум мастеров на экране**: 5
- **Фиксированная ширина при скролле**: 250px

### После изменения:
- **Минимальная ширина**: 200px (-20%)
- **Дефолтная ширина**: 240px (-20%)
- **Максимум мастеров на экране**: 6 (+1)
- **Фиксированная ширина при скролле**: 200px (-20%)

## Код изменений

```typescript
// Calculate column width based on number of working employees (20% narrower)
const getEmployeeColumnWidth = useMemo(() => {
    const workingEmployeeCount = employees.length;
    const maxEmployeesPerScreen = 6; // Увеличиваем до 6 из-за более узких колонок
    
    if (workingEmployeeCount === 0) {
        return 240; // Default width (was 300, now 20% smaller)
    }
    
    if (workingEmployeeCount <= maxEmployeesPerScreen) {
        // Stretch to full width when 6 or fewer employees
        const availableWidth = window.innerWidth - 80 - 40;
        const columnWidth = Math.max(200, Math.floor(availableWidth / workingEmployeeCount)); // Min 200px
        return columnWidth;
    } else {
        // Fixed width when more than 6 employees
        const fixedWidth = 200; // Fixed width 200px (was 250px)
        return fixedWidth;
    }
}, [employees.length]);
```

## Результат

### ✅ Улучшения:
- **Больше мастеров на экране** - теперь помещается до 6 мастеров без скролла
- **Эффективное использование пространства** - более компактное отображение
- **Сохранение читаемости** - минимальная ширина 200px обеспечивает удобочитаемость
- **Адаптивность** - логика растягивания и скролла работает аналогично

### 📊 Сравнение поведения:

| Ширина экрана | Мастеров (старое) | Мастеров (новое) | Улучшение |
|---------------|-------------------|-------------------|-----------|
| 1366px | 5 без скролла | 6 без скролла | +1 мастер |
| 1920px | 5 растянуто | 6 растянуто | +1 мастер |
| Любая | 6+ со скроллом | 7+ со скроллом | +1 порог |

### 🎯 Практическая польза:
- Пользователи видят больше мастеров одновременно
- Реже нужен горизонтальный скролл
- Более компактный интерфейс для салонов с большим количеством мастеров
- Сохранена функциональность drag&drop и sticky заголовков

### 🔧 Техническая реализация:
- Простое изменение констант без нарушения архитектуры
- Сохранение responsive поведения
- Мемоизация расчетов для производительности
- Обратная совместимость с существующим функционалом
