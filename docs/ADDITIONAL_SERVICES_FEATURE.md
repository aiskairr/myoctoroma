# Дополнительные услуги в новом календаре

## Обзор

Добавлена улучшенная система дополнительных услуг в новый календарь на основе анализа DailyCalendar. Система позволяет добавлять к основной записи дополнительные услуги с автоматическим расчетом времени и стоимости.

## Основные улучшения по сравнению с DailyCalendar

### 1. **Интегрированная система создания**
- ✅ Дополнительные услуги создаются сразу вместе с основной записью
- ✅ Автоматическое связывание через поле `motherId`
- ✅ Последовательное расположение по времени

### 2. **Улучшенный пользовательский интерфейс**
- 🎨 Современный дизайн с цветовой индикацией
- 📊 Отображение общего времени и стоимости в реальном времени
- 🔧 Редактирование длительности прямо в интерфейсе
- 🚫 Исключение уже добавленных услуг из списка

### 3. **Автоматические расчеты**
- ⏱️ Автоматический расчет времени начала дополнительных услуг
- 💰 Динамический расчет общей стоимости
- 🔄 Пересчет при изменении длительности

## Технические детали

### Новые интерфейсы

```typescript
interface Appointment {
    // ... existing fields
    price?: number;
    motherId?: string; // ID родительской записи для дополнительных услуг
    childIds?: string[]; // ID дочерних дополнительных услуг
    isAdditionalService?: boolean; // Флаг дополнительной услуги
    serviceId?: number; // ID услуги из справочника
}

interface AdditionalService {
    id: number;
    serviceId: number;
    serviceName: string;
    duration: number;
    price: number;
}
```

### Ключевые функции

#### 1. Расчет общей длительности
```typescript
const calculateTotalDuration = useCallback((baseAppointment?: Partial<Appointment> | null) => {
    const mainDuration = baseAppointment?.duration || 0;
    const additionalDuration = additionalServices.reduce((sum, service) => sum + service.duration, 0);
    return mainDuration + additionalDuration;
}, [additionalServices]);
```

#### 2. Расчет общей стоимости
```typescript
const calculateTotalPrice = useCallback((baseAppointment?: Partial<Appointment> | null) => {
    const mainPrice = baseAppointment?.price || 0;
    const additionalPrice = additionalServices.reduce((sum, service) => sum + service.price, 0);
    return mainPrice + additionalPrice;
}, [additionalServices]);
```

#### 3. Создание связанных записей
```typescript
// Создание дополнительных услуг после основной записи
for (const [index, service] of additionalServices.entries()) {
    // Расчет времени начала
    let totalPreviousDuration = duration;
    for (let i = 0; i < index; i++) {
        totalPreviousDuration += additionalServices[i].duration;
    }
    
    additionalStartTime = minutesToTime(timeToMinutes(selectedTimeSlot) + totalPreviousDuration);
    
    // Создание дополнительной записи с motherId
    const additionalTaskData = {
        // ... task data
        motherId: newTask.id // Связь с основной записью
    };
}
```

## Пользовательский интерфейс

### 1. **Секция дополнительных услуг**
```tsx
{/* Additional Services Section */}
<div className="border-t pt-4">
    <div className="flex items-center justify-between mb-3">
        <label>Дополнительные услуги</label>
        <div>Общее время: {calculateTotalDuration()} мин</div>
    </div>
    {/* Список дополнительных услуг */}
    {/* Добавление новой услуги */}
    {/* Общая стоимость */}
</div>
```

### 2. **Карточка дополнительной услуги**
- 📎 Иконка для визуального разделения
- ⏱️ Редактируемая длительность
- 💰 Автоматический расчет цены
- ❌ Кнопка удаления

### 3. **Селектор услуг**
- 🔍 Фильтрация уже добавленных услуг
- 📋 Отображение длительности и цены
- ➕ Кнопка быстрого добавления

## Логика работы

### Создание записи с дополнительными услугами:

1. **Основная запись** создается с выбранным временем
2. **Дополнительные услуги** создаются последовательно:
   - 1-я доп. услуга начинается сразу после основной
   - 2-я доп. услуга начинается после 1-й
   - И так далее...
3. **Связывание записей** через `motherId`
4. **Обновление календаря** с учетом общей длительности

### Расчет времени:
```
Основная услуга:     09:00 - 10:00 (60 мин)
Доп. услуга 1:      10:00 - 10:30 (30 мин) 
Доп. услуга 2:      10:30 - 11:00 (30 мин)
Общее время:        09:00 - 11:00 (120 мин)
```

## Преимущества новой системы

### ✅ **Функциональные улучшения**
- Создание всех услуг одним действием
- Автоматическое связывание записей
- Реальное время обновления расчетов
- Валидация пересечений времени

### 🎨 **UX улучшения**
- Интуитивный интерфейс
- Визуальная обратная связь
- Быстрое редактирование
- Четкое отображение общих показателей

### 🔧 **Техническая оптимизация**
- Мемоизация расчетов
- Типизированные интерфейсы
- Обработка ошибок
- Согласованность данных

## Будущие улучшения

1. **Редактирование существующих записей** с дополнительными услугами
2. **Групповые операции** над связанными записями
3. **Шаблоны услуг** для быстрого добавления пакетов
4. **Визуализация связей** в календаре
5. **Отчеты** по дополнительным услугам
