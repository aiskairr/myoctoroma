# Миграция логики мастеров на новый календарь

## Обзор

Документ описывает миграцию функционала для пользователей с ролью `master` со старых календарных компонентов на новый унифицированный календарь `schedule-calendar` (time-schedule.tsx).

## Дата обновления
20 октября 2025

## Проблема

У нас были отдельные календарные компоненты для мастеров:
- `MobileMasterCalendar.tsx` - мобильная версия календаря для мастеров
- `MasterCalendar.tsx` - десктоп версия календаря для мастеров  
- `DailyCalendar.tsx` - старый календарь

При попытке входа на другие страницы мастера видели сообщение "Доступ закрыт". 

Поскольку теперь используется новый унифицированный календарь `/crm/calendar`, необходимо было продублировать логику для мастеров и удалить старые компоненты.

## Решение

### 1. Добавлена логика для роли master в новый календарь

#### Файл: `src/pages/Calendar/components/time-schedule.tsx`

**Фильтрация мастеров (строки 282-321):**
```typescript
const employees: Employee[] = useMemo(() => {
    console.log('🔄 Building employees list with working dates...');
    console.log('  - Current user role:', user?.role, 'master_id:', user?.master_id);
    
    // Фильтруем мастеров для роли "master"
    let filteredMasters = mastersData;
    if (user?.role === 'master' && user?.master_id) {
        filteredMasters = mastersData.filter(master => master.id === user.master_id);
        console.log('  - Filtered for master role, showing only master_id:', user.master_id);
    }
    
    // ... остальная логика
}, [mastersData, masterWorkingDates, user?.role, user?.master_id]);
```

**Ключевые изменения:**
- Для мастеров показывается только их собственный столбец в календаре
- Используется `user.master_id` для фильтрации

**Ограничения для кнопок управления:**

1. **Кнопка "Добавить мастера" (строки 1633-1643):**
```typescript
{/* Кнопка добавления мастера доступна только для админов */}
{user?.role !== 'master' && (
    <Dialog open={isAddEmployeeOpen} onOpenChange={setIsAddEmployeeOpen}>
        <DialogTrigger asChild>
            <button className="flex items-center gap-2 px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors font-medium">
                <Plus size={18} />
                {t('calendar.add_employee')}
            </button>
        </DialogTrigger>
    // ...
)}
```

2. **Кнопка "Удалить мастера" (строки 1904-1912):**
```typescript
{/* Кнопка удаления доступна только для админов */}
{user?.role !== 'master' && (
    <button
        onClick={() => handleRemoveEmployee(employee.id)}
        className="p-1 text-gray-400 hover:text-red-500 opacity-0 group-hover:opacity-100 transition-all rounded flex-shrink-0"
        title={t('calendar.delete_employee')}
    >
        <X size={16} />
    </button>
)}
```

**Ограничения drag & drop (строки 851-856):**
```typescript
const handleMouseDown = useCallback((e: React.MouseEvent, appointment: Appointment, action: 'drag' | 'resize-top' | 'resize-bottom') => {
    e.preventDefault();
    e.stopPropagation();
    
    // Для роли master запрещаем перетаскивание записей других мастеров
    if (user?.role === 'master' && user?.master_id && appointment.employeeId !== user.master_id.toString()) {
        console.log('🚫 Master cannot drag appointments of other masters');
        return;
    }
    
    // ... остальная логика
}, []);
```

### 2. Обновлена логика доступа

#### Файл: `src/components/SimpleProtectedRoute.tsx`

**Добавлено ограничение доступа для мастеров (строки 40-56):**
```typescript
// Для мастеров ограничиваем доступ только к календарю
if (user?.role === 'master') {
    const currentPath = window.location.pathname;
    const allowedPaths = ['/crm/calendar', '/master/calendar', '/settings'];
    
    if (!allowedPaths.includes(currentPath)) {
        window.location.href = "/master/calendar";
        return (
            <div className="min-h-screen flex items-center justify-center bg-gray-50">
                <div className="text-center">
                    <Loader2 className="h-8 w-8 animate-spin text-blue-600 mx-auto mb-4" />
                    <p className="text-gray-600">Перенаправление на календарь...</p>
                </div>
            </div>
        );
    }
}
```

**Разрешенные страницы для мастеров:**
- `/master/calendar` - календарь мастера (основной путь)
- `/crm/calendar` - альтернативный путь к календарю
- `/settings` - настройки профиля

### 3. Удалены старые компоненты

**Удаленные файлы:**
- `src/pages/MobileMasterCalendar.tsx`
- `src/pages/MasterCalendar.tsx`
- `src/pages/DailyCalendar.tsx`

**Удаленные роуты из `App.tsx`:**
- `/crm/calendarscreen` → `DailyCalendar`
- `/master-calendar` → `MasterCalendar` (старый)

**Оставшиеся роуты:**
- `/crm/calendar` → `CalendarScreen` (новый унифицированный календарь)
- `/master/calendar` → `CalendarScreen` (тот же календарь, для мастеров)

### 4. Обновлен MobileApp

#### Файл: `src/pages/MobileApp.tsx`

Мобильное приложение теперь перенаправляет авторизованных мастеров на календарь мастера `/master/calendar` вместо использования отдельного мобильного компонента.

```typescript
// Если пользователь уже авторизован через основную систему и является мастером,
// перенаправляем на календарь мастера
useEffect(() => {
    if (isAuthenticated && user?.role === 'master') {
        window.location.href = '/master/calendar';
        return;
    }
    setIsLoading(false);
}, [isAuthenticated, user]);
```

## Функциональность для мастеров

### Что могут делать мастера:

1. ✅ Просматривать свое расписание в календаре
2. ✅ Видеть только свои задачи/записи
3. ✅ Редактировать свои записи через диалог редактирования
4. ✅ Перетаскивать и изменять длительность своих записей (drag & drop)
5. ✅ Создавать новые записи для себя
6. ✅ Доступ к странице настроек `/settings`

### Что НЕ могут делать мастера:

1. ❌ Видеть записи других мастеров
2. ❌ Добавлять других мастеров в рабочий день
3. ❌ Удалять мастеров из расписания
4. ❌ Перетаскивать записи других мастеров
5. ❌ Доступ к другим страницам системы (автоматический редирект на `/crm/calendar`)

## Технические детали

### Проверка роли пользователя

```typescript
user?.role === 'master'
user?.master_id // ID мастера в системе
```

### Фильтрация данных

```typescript
// Фильтрация мастеров
let filteredMasters = mastersData;
if (user?.role === 'master' && user?.master_id) {
    filteredMasters = mastersData.filter(master => master.id === user.master_id);
}

// Фильтрация записей происходит автоматически через employeeId
appointment.employeeId === user.master_id.toString()

// Для мастеров показываем календарь всегда, даже если они не отмечены как "работающие" в конкретный день
const isWorking = user?.role === 'master' ? true : !!workingDate;
```

### Важно: Отображение календаря мастера

Для роли `master` календарь отображается **всегда**, независимо от того, добавлен ли мастер в рабочий день через систему или нет. Это позволяет мастерам видеть свой календарь и управлять записями в любой день.

Для администраторов показываются только мастера, которые отмечены как работающие в выбранную дату.

## Миграция для пользователей

### Для мастеров:

1. Вход в систему остается прежним
2. После входа автоматическое перенаправление на `/master/calendar`
3. В календаре отображается только их колонка
4. Календарь отображается всегда, независимо от рабочего дня
5. Все функции редактирования доступны только для своих записей

### Для администраторов:

1. Полный доступ к календарю со всеми мастерами
2. Возможность добавления/удаления мастеров
3. Управление записями всех мастеров

## Тестирование

### Тест-кейсы для роли master:

1. ✅ Авторизация мастера перенаправляет на `/master/calendar`
2. ✅ В календаре видна только одна колонка с именем мастера
3. ✅ Календарь отображается даже если мастер не добавлен в рабочий день
4. ✅ Попытка доступа к другим страницам перенаправляет на `/master/calendar`
5. ✅ Кнопка "Добавить мастера" скрыта
6. ✅ Кнопка "Удалить" у заголовка мастера скрыта
7. ✅ Drag & drop работает только для своих записей
8. ✅ Клик на запись открывает диалог редактирования
9. ✅ Доступ к `/settings` разрешен
10. ✅ MasterSidebar корректно работает с `/master/calendar`

### Проверка логов:

В консоли браузера при загрузке календаря для мастера должны быть логи:
```
🔄 Building employees list with working dates...
  - Current user role: master master_id: [ID]
  - Filtered for master role, showing only master_id: [ID]
  - Total working employees: 1 out of [TOTAL]
```

## Обратная совместимость

- Старые роуты автоматически не работают (удалены)
- API эндпоинты остались без изменений
- Структура данных не изменилась

## Будущие улучшения

1. Добавить возможность мастеру отмечать перерывы
2. Push-уведомления о новых записях
3. Статистика работы мастера
4. Мобильная адаптация календаря для лучшего UX на телефонах

## Авторы

- Команда разработки Elitaroma Frontend
- Дата: 20 октября 2025
