# Улучшение UI календаря мастера для мобильных устройств

**Дата:** 20 октября 2025 г.  
**Файлы:** 
- `src/pages/Calendar/MasterCalendarView.tsx`
- `src/contexts/LocaleContext.tsx`

## Проблемы

1. **Отсутствует перевод:** `nav.master_calendar` не был переведен
2. **Не помещается на мобильных:** Кнопки "Предыдущий день" и "Следующий день" слишком длинные для маленьких экранов

## Решение

### 1. Добавлены переводы для nav.master_calendar

**Русский (ru):**
```typescript
'nav.master_calendar': 'Мой календарь',
```

**Кыргызский (ky):**
```typescript
'nav.master_calendar': 'Менин календарым',
```

**Английский (en):**
```typescript
'nav.master_calendar': 'My Calendar',
```

### 2. Адаптивная навигация по датам

#### Было:
```tsx
<Button variant="outline" size="sm" onClick={goToPreviousDay}>
  <ChevronLeft className="h-4 w-4 mr-1" />
  Предыдущий день
</Button>

<Button variant="outline" size="sm" onClick={goToNextDay}>
  Следующий день
  <ChevronRight className="h-4 w-4 ml-1" />
</Button>
```

**Проблема:** Текст занимает много места на мобильных экранах

#### Стало:
```tsx
{/* На мобильных только иконка, на десктопе текст + иконка */}
<Button 
  variant="outline" 
  size="sm" 
  onClick={goToPreviousDay}
  className="flex-shrink-0"
>
  <ChevronLeft className="h-4 w-4" />
  <span className="hidden sm:inline ml-1">Предыдущий день</span>
</Button>

<Button 
  variant="outline" 
  size="sm" 
  onClick={goToNextDay}
  className="flex-shrink-0"
>
  <span className="hidden sm:inline mr-1">Следующий день</span>
  <ChevronRight className="h-4 w-4" />
</Button>
```

**Результат:**
- 📱 **Мобильные (< 640px):** Показываются только стрелки `←` и `→`
- 💻 **Десктоп (≥ 640px):** Показываются текст + стрелки "← Предыдущий день" и "Следующий день →"

### 3. Улучшена адаптивность блока даты

```tsx
<div className="text-center flex-grow min-w-0">
  <h2 className="text-lg sm:text-xl font-semibold truncate">
    {format(selectedDate, "yyyy-MM-dd")}
  </h2>
  <p className="text-xs sm:text-sm text-gray-600">
    {format(selectedDate, "EEEE")}
  </p>
</div>
```

**Изменения:**
- `flex-grow` - занимает доступное пространство
- `min-w-0` - позволяет сжиматься при необходимости
- `truncate` - обрезает длинный текст с многоточием
- `text-lg sm:text-xl` - меньший размер на мобильных
- `text-xs sm:text-sm` - меньший размер дня недели на мобильных

### 4. Адаптивные кнопки "Сегодня" и "Обновить"

```tsx
<div className="flex justify-center gap-2 flex-wrap">
  {!isToday && (
    <Button variant="outline" size="sm" onClick={goToToday}>
      <CalendarIcon className="w-4 h-4 mr-1" />
      <span className="hidden sm:inline">Сегодня</span>
    </Button>
  )}
  <Button variant="outline" size="sm" onClick={() => refetch()}>
    <RefreshCw className="w-4 h-4" />
    <span className="hidden sm:inline ml-1">Обновить</span>
  </Button>
</div>
```

**Результат:**
- 📱 **Мобильные:** Только иконки (календарь, обновление)
- 💻 **Десктоп:** Иконки + текст

### 5. Общие улучшения контейнера

```tsx
<div className="flex items-center justify-between mb-4 gap-2">
  {/* Кнопки и дата */}
</div>
```

**Добавлено:**
- `gap-2` - минимальный отступ между элементами
- `flex-shrink-0` на кнопках - предотвращает сжатие кнопок
- `flex-wrap` - позволяет переносить элементы на новую строку при необходимости

## Визуальный результат

### Мобильный вид (< 640px):
```
┌─────────────────────────────────┐
│  ←     2025-10-20      →       │
│        Monday                    │
│                                  │
│    📅      🔄                    │
└─────────────────────────────────┘
```

### Десктоп вид (≥ 640px):
```
┌────────────────────────────────────────────────┐
│  ← Предыдущий день   2025-10-20   Следующий день →  │
│                      Monday                          │
│                                                      │
│         📅 Сегодня      🔄 Обновить                 │
└────────────────────────────────────────────────┘
```

## Tailwind CSS классы

### Адаптивные классы:
- `hidden` - скрыть элемент
- `sm:inline` - показать как inline на экранах ≥ 640px
- `text-lg` - размер текста на мобильных
- `sm:text-xl` - размер текста на десктопе
- `flex-shrink-0` - не сжимать элемент
- `flex-grow` - занимать доступное пространство
- `min-w-0` - разрешить сжатие до минимума
- `truncate` - обрезать текст с `...`
- `flex-wrap` - перенос на новую строку

## Тестирование

1. **Откройте DevTools** (F12)
2. **Переключите на мобильный вид** (Ctrl+Shift+M)
3. **Проверьте разные размеры:**
   - iPhone SE (375px) - только иконки
   - iPad (768px) - текст + иконки
   - Desktop (1920px) - полный текст

4. **Проверьте переводы:**
   - Смените язык на русский/кыргызский/английский
   - Убедитесь, что "nav.master_calendar" переведен

## Преимущества

✅ **Компактность** - помещается на маленьких экранах  
✅ **Интуитивность** - стрелки понятны без текста  
✅ **Адаптивность** - автоматически подстраивается под размер экрана  
✅ **Читаемость** - на больших экранах показывается полный текст  
✅ **Переведено** - поддержка всех языков приложения

## Связанные документы

- `MASTER_CALENDAR_DISPLAY_FIX.md` - исправление отображения данных
- `AUTH_CONTEXT_MASTERID_FIX.md` - исправление masterId
