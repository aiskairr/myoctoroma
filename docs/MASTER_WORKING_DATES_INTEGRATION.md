# Интеграция API рабочих дат мастеров

## Что было сделано

Интегрирован эндпоинт `/api/crm/master-working-dates` в календарь для динамического определения рабочих дней и часов мастеров.

## Изменения в коде

### 1. Создан новый хук `use-master-working-dates.ts`

```typescript
export interface MasterWorkingDate {
  id: number;
  master_id: number;
  master_name: string;
  work_date: string;
  start_time: string;
  end_time: string;
  branch_id: string;
  is_active: boolean;
}

export const useMasterWorkingDates = (date?: string, branchId?: string)
export const useMasterWorkingHoursForDate = (masterId: number, date: string, branchId?: string)
```

### 2. Обновлен компонент `time-schedule.tsx`

- **Новый тип Employee**: Добавлены поля `isWorking` и `workingDate`
- **Динамические рабочие часы**: Теперь используются данные из API вместо констант
- **Обновлены валидации**: 
  - `isWithinWorkingHours()` - проверяет `employee.isWorking`
  - `doesAppointmentFitWorkingHours()` - учитывает состояние работы мастера
- **Состояния загрузки**: Добавлена обработка загрузки и ошибок API

### 3. Логика работы

1. При выборе даты отправляется запрос на `/api/crm/master-working-dates?date=YYYY-MM-DD&branchId=X`
2. Полученные данные используются для:
   - Определения рабочих часов каждого мастера
   - Блокировки нерабочих временных слотов
   - Валидации при создании/перемещении записей

## Параметры API

- `date` (обязательный) - дата в формате `YYYY-MM-DD`
- `branchId` (опциональный) - ID филиала для фильтрации

## Поведение

- **Мастер работает**: Отображается в календаре с заданными рабочими часами
- **Мастер не работает**: `isWorking: false`, временные слоты заблокированы
- **Нет данных**: Используются fallback значения из профиля мастера
- **Ошибка API**: Отображается сообщение об ошибке

## Кеширование

- Данные кешируются на 5 минут
- Автоматическое обновление при смене даты или филиала
- Отключено обновление при фокусе окна

## Логирование

В консоли доступны подробные логи:
- `🔄 Building employees list with working dates...`
- Информация о рабочих часах каждого мастера
- Источник данных (API или fallback)
