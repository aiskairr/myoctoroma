# Обновление страницы Chats - Интеграция с /api/whatsapp/chats-list

## Дата: 13 ноября 2025 г.

## Описание изменений

Страница `/chats` обновлена для использования нового API endpoint `/api/whatsapp/chats-list`, который возвращает список всех WhatsApp чатов с метаданными (без полных массивов сообщений).

## 🎯 Основные изменения

### 1. Новый API Endpoint

**Старая версия:**
- Загружала клиентов из `/api/clients?branchId=...`
- Требовала фильтрацию по филиалу
- Не было информации о WhatsApp чатах

**Новая версия:**
- Загружает чаты из `/api/whatsapp/chats-list?page=1&limit=50`
- Получает метаданные чатов (без полных сообщений)
- Отображает превью последнего сообщения

### 2. Интерфейс WhatsAppChatItem

```typescript
interface WhatsAppChatItem {
  chatId: string | null;
  contactNumber: string;
  contactName: string | null;
  unreadCount: number;
  lastMessageTime: string;
  lastMessage: string;
  messagesCount: number;
}
```

### 3. UI Улучшения

**Карточки чатов теперь показывают:**
- 📱 Имя контакта / номер телефона
- 💬 Превью последнего сообщения
- 🔴 Badge с количеством непрочитанных (если > 0)
- 📊 Общее количество сообщений
- ⏰ Время последнего сообщения (умное форматирование)
- 🟢 Зеленая левая граница для визуального выделения

**Умное форматирование времени:**
- "Только что" - меньше 1 минуты
- "5 мин назад" - меньше 60 минут
- "3 ч назад" - меньше 24 часов
- "2 д назад" - меньше 7 дней
- "dd.MM.yyyy HH:mm" - старше 7 дней

## 📝 Изменения в коде

### Chats.tsx

**Удалено:**
- ❌ Зависимость от `useBranch`
- ❌ Интерфейс `Client` и `ClientsResponse`
- ❌ Функция `getClientDisplayName`
- ❌ Логика фильтрации по филиалам
- ❌ Отображение username, tasks_count, isActive

**Добавлено:**
- ✅ Интерфейс `WhatsAppChatItem`
- ✅ Интерфейс `ChatsListResponse` с пагинацией
- ✅ Функция `getChatDisplayName`
- ✅ Умное форматирование времени
- ✅ Локальная фильтрация чатов по поиску
- ✅ Отображение непрочитанных сообщений
- ✅ Превью последнего сообщения

### LocaleContext.tsx

**Добавлены новые ключи переводов:**

```typescript
// WhatsApp
'whatsapp.messages': 'сообщений' | 'билдирүүлөр' | 'messages'
'whatsapp.just_now': 'Только что' | 'Азыр эле' | 'Just now'

// Time ago
'minutes_ago': 'мин назад' | 'мүн мурун' | 'min ago'
'hours_ago': 'ч назад' | 'саат мурун' | 'h ago'
'days_ago': 'д назад' | 'күн мурун' | 'd ago'
```

## 🔄 Поток данных

### 1. Загрузка списка чатов

```typescript
// Запрос к API
GET /api/whatsapp/chats-list?page=1&limit=50

// Response
{
  "success": true,
  "data": [
    {
      "chatId": null,
      "contactNumber": "996555880301",
      "contactName": null,
      "unreadCount": 0,
      "lastMessageTime": "2025-11-13T10:55:32.886Z",
      "lastMessage": "Здравствуйте!",
      "messagesCount": 4
    }
  ],
  "pagination": {
    "total": 1,
    "page": 1,
    "limit": 50,
    "totalPages": 1,
    "hasNextPage": false,
    "hasPrevPage": false
  }
}
```

### 2. Локальная фильтрация

```typescript
// Поиск по номеру, имени или тексту сообщения
const filtered = chats.filter(chat => 
  chat.contactNumber.includes(searchQuery) ||
  chat.contactName?.toLowerCase().includes(searchQuery.toLowerCase()) ||
  chat.lastMessage.toLowerCase().includes(searchQuery.toLowerCase())
);
```

### 3. Открытие чата

```typescript
// При клике на карточку
handleOpenChat(chat) {
  setSelectedChat({
    phone: chat.contactNumber,
    name: getChatDisplayName(chat),
    id: 0 // WhatsApp чаты не имеют ID клиента
  });
  setIsWhatsAppChatOpen(true);
}

// Открывается WhatsAppChat компонент
<WhatsAppChat
  isOpen={isWhatsAppChatOpen}
  onClose={() => setIsWhatsAppChatOpen(false)}
  phone={selectedChat.phone}
  clientName={selectedChat.name}
  clientId={selectedChat.id}
/>
```

### 4. Загрузка полной истории

```typescript
// Внутри WhatsAppChat компонента
GET /api/whatsapp/chats?phone=996555880301&page=1&limit=20

// Возвращает полную историю сообщений
```

## 🎨 UI Компоненты

### Структура карточки чата

```jsx
<Card
  className="cursor-pointer hover:shadow-lg transition-all duration-200 hover:scale-105 border-l-4 border-l-green-500"
  onClick={() => handleOpenChat(chat)}
>
  <CardContent className="p-4">
    {/* Заголовок с именем и badge непрочитанных */}
    <div className="flex justify-between items-start">
      <div>
        <h3>{getChatDisplayName(chat)}</h3>
        <div><Phone /> {chat.contactNumber}</div>
      </div>
      {chat.unreadCount > 0 && (
        <Badge variant="destructive">{chat.unreadCount}</Badge>
      )}
    </div>
    
    {/* Превью последнего сообщения */}
    <div className="text-sm text-muted-foreground line-clamp-2 bg-muted/30 p-2 rounded">
      {chat.lastMessage}
    </div>
    
    {/* Футер: количество сообщений и время */}
    <div className="flex justify-between">
      <Badge>
        <MessageCircle /> {chat.messagesCount} сообщений
      </Badge>
      <span>{formatDate(chat.lastMessageTime)}</span>
    </div>
  </CardContent>
</Card>
```

## 📊 Преимущества новой версии

### Производительность
- ⚡ Быстрая загрузка списка (только метаданные, без полных сообщений)
- ⚡ Меньше трафика (API не возвращает массивы сообщений)
- ⚡ Локальная фильтрация (без дополнительных запросов)

### UX
- 👁️ Превью последнего сообщения
- 🔴 Индикатор непрочитанных сообщений
- ⏰ Умное отображение времени
- 🔍 Быстрый поиск по всем полям
- 🎨 Визуально привлекательные карточки

### Функциональность
- 📱 Поддержка пагинации (ready for future)
- 🔄 Легкое обновление списка
- 📊 Отображение статистики (количество сообщений)
- 🎯 Фокус на WhatsApp чатах (не смешано с клиентами)

## 🚀 Использование

### Открытие страницы
1. Перейдите на `/chats`
2. Увидите список всех WhatsApp чатов
3. Используйте поиск для фильтрации
4. Нажмите на карточку чата
5. Откроется полная история сообщений

### Поиск
- По номеру телефона: `996555`
- По имени контакта: `Иван`
- По тексту сообщения: `Здравствуйте`

### Индикаторы
- **Красный badge** - количество непрочитанных сообщений
- **Зеленая граница** - активный WhatsApp чат
- **Время** - когда было последнее сообщение
- **Счетчик** - общее количество сообщений

## 🔍 Тестирование

### Чек-лист функциональности:

- [x] Загрузка списка чатов из `/api/whatsapp/chats-list`
- [x] Отображение карточек с превью
- [x] Поиск по номеру, имени, тексту сообщения
- [x] Умное форматирование времени
- [x] Badge непрочитанных сообщений
- [x] Клик открывает полную историю чата
- [x] Переводы на 3 языка
- [x] Responsive design (1/2/3 колонки)

### Проверка сборки:

```bash
npm run build
# ✅ Успешно собрано без ошибок
# Bundle size: 2,665.73 kB (gzip: 639.93 kB)
```

## 📈 Будущие улучшения

### Планируемые функции:

- [ ] Пагинация для больших списков чатов
- [ ] Real-time обновления через WebSocket
- [ ] Фильтры (только непрочитанные, по дате)
- [ ] Сортировка (по времени, по имени, по количеству)
- [ ] Массовая отметка как прочитанное
- [ ] Архивация чатов
- [ ] Избранные чаты
- [ ] Групповые чаты

## 🔗 Связанные документы

- [WHATSAPP_CHAT_INTEGRATION.md](./WHATSAPP_CHAT_INTEGRATION.md) - Общая документация по WhatsApp интеграции
- [CHATS_PAGE_WHATSAPP_MIGRATION.md](./CHATS_PAGE_WHATSAPP_MIGRATION.md) - Первая версия миграции

## 📝 Технические детали

### API Endpoints

**Используемые:**
- `GET /api/whatsapp/chats-list` - Список чатов (метаданные)
- `GET /api/whatsapp/chats?phone=...` - Полная история чата
- `POST /api/whatsapp/send` - Отправка сообщения

**НЕ используемые:**
- ~~`GET /api/clients`~~ - Больше не нужен для этой страницы

### Зависимости

**React Hooks:**
- `useState` - Управление состоянием
- `useEffect` - Загрузка данных и поиск
- `useToast` - Уведомления
- `useLocale` - Переводы

**UI Components:**
- `Card`, `CardContent`, `CardHeader`, `CardTitle`
- `Badge` (default, destructive, outline)
- `Input` - Поиск
- `WhatsAppChat` - Диалог чата

**Icons:**
- `Search` - Поиск
- `Phone` - Номер телефона
- `Loader2` - Загрузка
- `MessageCircle` - Сообщения

## ✅ Заключение

Страница `/chats` успешно обновлена для использования нового API `/api/whatsapp/chats-list`. Новая версия обеспечивает:

- ⚡ Лучшую производительность
- 🎨 Улучшенный UI/UX
- 📊 Больше информации (превью, непрочитанные)
- 🔍 Удобный поиск
- 🌐 Полная локализация

**Статус:** ✅ Готово к production  
**Версия:** 2.1.0  
**Дата:** 13 ноября 2025 г.  
**Commit:** Ready for deployment
