# 📱 Адаптивные интервалы обновления для Calendar

## ✅ Реализовано

Создана система адаптивных интервалов обновления данных в зависимости от типа устройства для оптимизации производительности и экономии батареи на мобильных устройствах.

## 🎯 Решение

### 1. Создан хук `useDevice` (`src/hooks/use-device.ts`)

**Функции:**
- `useIsMobile()` - определяет мобильное устройство (< 768px)
- `useRefetchSettings()` - возвращает настройки обновления

**Настройки по устройствам:**

| Параметр | Desktop | Mobile |
|----------|---------|--------|
| `refetchInterval` | 30 сек | 2 мин (120 сек) |
| `staleTime` | 30 сек | 2 мин (120 сек) |
| `refetchOnWindowFocus` | ✅ true | ✅ true |
| `refetchIntervalInBackground` | ❌ false | ❌ false |

### 2. Обновлен `useTasks` (`src/hooks/use-tasks.ts`)

```typescript
// Автоматически подстраивается под устройство
const refetchSettings = useRefetchSettings();

// В React Query:
staleTime: refetchSettings.staleTime,
refetchInterval: refetchSettings.refetchInterval,
refetchIntervalInBackground: refetchSettings.refetchIntervalInBackground,
refetchOnWindowFocus: refetchSettings.refetchOnWindowFocus,
```

## 📊 Сравнение до/после

### ❌ Было (проблема):
- **Desktop и Mobile:** обновление каждые 30 секунд
- **Мобильные устройства:** частые запросы → разряд батареи
- **Safari iOS:** "прыжки" UI при обновлении

### ✅ Стало (решение):
- **Desktop:** обновление каждые 30 секунд (быстро)
- **Mobile:** обновление каждые 2 минуты (экономия)
- **Оба:** обновление при возврате на вкладку
- **Оба:** НЕТ обновлений в фоновом режиме

## 🔋 Экономия ресурсов

### Desktop (без изменений):
- Запросов в час: **120** (каждые 30 сек)
- Назначение: быстрые обновления для активной работы

### Mobile (оптимизировано):
- **Было:** 120 запросов/час
- **Стало:** 30 запросов/час
- **Экономия:** 75% меньше запросов! 🎉

## 🚀 Преимущества

### Для мобильных устройств:
- ✅ Экономия батареи (75% меньше запросов)
- ✅ Экономия трафика
- ✅ Меньше "прыжков" UI
- ✅ Плавная работа в Safari iOS
- ✅ Быстрая работа приложения

### Для desktop:
- ✅ Быстрые обновления (30 сек)
- ✅ Актуальные данные для операторов
- ✅ Без задержек

### Общие:
- ✅ Автоматическое определение устройства
- ✅ Обновление при возврате на вкладку
- ✅ Единая кодовая база

## 🧪 Как протестировать

### На Desktop:
1. Откройте календарь
2. Откройте DevTools → Network
3. Увидите запросы каждые 30 секунд

### На Mobile:
1. Откройте календарь на телефоне
2. Запросы будут каждые 2 минуты
3. При переключении вкладок - моментальное обновление

### Проверка адаптивности:
```javascript
// В консоли браузера:
window.innerWidth // Если < 768 - mobile режим
```

## 📁 Измененные файлы

1. **`src/hooks/use-device.ts`** ✨ НОВЫЙ
   - Определение типа устройства
   - Настройки обновления

2. **`src/hooks/use-tasks.ts`** 🔧 ОБНОВЛЕН
   - Использует `useRefetchSettings()`
   - Адаптивные интервалы

3. **`src/hooks/use-task.ts`** 🔧 УЖЕ ОБНОВЛЕН (ранее)
   - Отключено авто-обновление для диалогов

4. **`src/pages/Dashboard.tsx`** 🔧 УЖЕ ОБНОВЛЕН (ранее)
   - Интервал увеличен до 60 секунд

5. **`src/pages/Calendar/components/advanced-schedule-migrated.tsx`** 🔧 УЖЕ ОБНОВЛЕН (ранее)
   - Интервал увеличен до 60 секунд

## 🎨 Автоматическое переключение

Система автоматически переключается между режимами:
- При изменении размера окна (resize)
- При повороте устройства
- При эмуляции мобильного в DevTools

Нет необходимости в ручной настройке!

## 📱 Режимы работы

### Desktop режим (≥ 768px):
```
Интервал: 30 секунд
↓
Быстрые обновления
↓
Для активной работы операторов
```

### Mobile режим (< 768px):
```
Интервал: 2 минуты
↓
Экономия батареи
↓
Для клиентов и мастеров
```

### При возврате на вкладку (оба):
```
Потеря фокуса → Возврат
↓
Автоматическое обновление
↓
Свежие данные
```

## 💡 Дополнительные возможности

Если нужно изменить интервалы, отредактируйте `src/hooks/use-device.ts`:

```typescript
export function useRefetchSettings() {
  const isMobile = useIsMobile();

  return {
    isMobile,
    // Можно настроить под свои нужды:
    refetchInterval: isMobile ? 1000 * 60 * 3 : 1000 * 20, // 3 мин / 20 сек
    staleTime: isMobile ? 1000 * 60 * 3 : 1000 * 20,
    // ...
  };
}
```

## 🎯 Результат

- 📱 **Mobile:** плавная работа, экономия батареи
- 💻 **Desktop:** быстрые обновления
- 🔄 **Автоматическая адаптация** под устройство
- ✅ **Единая кодовая база** для всех платформ

---

**Дата обновления:** 26 ноября 2025  
**Статус:** ✅ Готово к продакшену  
**Breakpoint:** 768px (стандарт Tailwind)
