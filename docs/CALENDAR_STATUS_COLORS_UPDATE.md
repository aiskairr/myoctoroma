# Обновление цветовой схемы и индикаторов оплаты в календаре

## Обзор изменений

Изменена логика отображения записей в календаре `time-schedule.tsx`:
- ✅ Цвета записей теперь зависят от статуса, а не от мастера
- ✅ Добавлена иконка монетки для неоплаченных записей
- ✅ Обновлен tooltip с информацией об оплате

## Цветовая схема по статусам

### Новые цвета границ записей:
- 🔵 **Запланировано** (`scheduled`) - Синий `#3B82F6`
- 🟢 **В процессе** (`in-progress`) - Зеленый `#10B981`
- ⚫ **Завершено** (`completed`) - Серый `#6B7280`
- 🔴 **Отменено** (`cancelled`) - Красный `#EF4444`

### Индикатор оплаты:
- 💰 **Неоплачено** - Иконка монетки (Coins) в правом верхнем углу записи
- ✅ **Оплачено** - Без дополнительных индикаторов

## Технические изменения

### 1. Обновлен интерфейс Appointment
```typescript
interface Appointment {
    // ... существующие поля
    paid?: string; // Статус оплаты: 'paid' или 'unpaid'
}
```

### 2. Новая функция getOverlapColor
```typescript
const getOverlapColor = useCallback((appointment: Appointment) => {
    const statusColors = {
        'scheduled': '#3B82F6',    // blue
        'in-progress': '#10B981',  // green  
        'completed': '#6B7280',    // gray
        'cancelled': '#EF4444'     // red
    };
    return statusColors[appointment.status] || '#3B82F6';
}, []);
```

### 3. Добавлена иконка оплаты
```tsx
{appointment.paid !== 'paid' && (
    <div className="absolute top-0 right-0 z-10">
        <Coins className="h-3 w-3 text-amber-500" />
    </div>
)}
```

### 4. Обновлен tooltip
- Добавлена информация о статусе оплаты
- Цветовая индикация (зеленый/красный)
- Иконка монетки для неоплаченных

## Интеграция с API

### Маппинг данных
Поле `paid` автоматически маппится из API данных:
```typescript
const appointment = {
    // ... другие поля
    paid: task.paid || 'unpaid' // Значение по умолчанию
};
```

### Поддерживаемые значения:
- `'paid'` - Запись оплачена
- `'unpaid'` - Запись не оплачена
- `undefined/null` - Считается как неоплаченная

## Визуальные улучшения

### До изменений:
- Цвета зависели от мастера
- Не было индикации статуса оплаты
- Сложно было различить статусы записей

### После изменений:
- ✅ Интуитивные цвета по статусам
- ✅ Четкая индикация неоплаченных записей
- ✅ Улучшенный tooltip с полной информацией
- ✅ Консистентность с DailyCalendar

## Совместимость

Изменения полностью обратно совместимы:
- Существующие записи без поля `paid` отображаются как неоплаченные
- Старые статусы продолжают работать
- API может не предоставлять поле `paid` - будет использоваться значение по умолчанию

## Статус
- ✅ **Завершено**: Цветовая схема по статусам
- ✅ **Завершено**: Иконка монетки для неоплаченных записей  
- ✅ **Завершено**: Обновленный tooltip
- ✅ **Завершено**: Интеграция с API данными
- ✅ **Протестировано**: Компиляция без ошибок
