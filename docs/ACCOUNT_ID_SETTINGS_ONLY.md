# Удаление дублирования accountID из редактирования филиала

**Дата:** 21 ноября 2025 г.  
**Статус:** ✅ Завершено

## Проблема

Поле `accountID` дублировалось в двух местах:
1. В диалоге **"Редактирование филиала"** (BranchSelector.tsx)
2. На странице **"Настройки"** → вкладка **"Чат-бот"** (Settings.tsx)

Это создавало путаницу и могло приводить к рассинхронизации данных.

## Решение

Убрали поле `accountID` из диалога редактирования филиала, оставив его **только в настройках чат-бота**, так как:
- accountID используется исключительно для WhatsApp Business API
- Логичнее редактировать настройки бота в одном месте вместе с другими параметрами бота
- Это упрощает интерфейс и снижает вероятность ошибок

## Изменения в коде

### 1. **src/components/BranchSelector.tsx**

#### Удален интерфейс accountID из EditBranchData:
```typescript
// БЫЛО:
interface EditBranchData {
  branches: string;
  address: string;
  phoneNumber: string;
  accountID?: string; // ❌ Удалено
}

// СТАЛО:
interface EditBranchData {
  branches: string;
  address: string;
  phoneNumber: string;
}
```

#### Удалена инициализация accountID:
```typescript
// БЫЛО:
const handleEditBranch = (branch: Branch) => {
  setEditingBranch(branch);
  setEditData({
    branches: branch.branches,
    address: branch.address,
    phoneNumber: branch.phoneNumber,
    accountID: branch.accountID || '' // ❌ Удалено
  });
  setEditDialog(true);
};

// СТАЛО:
const handleEditBranch = (branch: Branch) => {
  setEditingBranch(branch);
  setEditData({
    branches: branch.branches,
    address: branch.address,
    phoneNumber: branch.phoneNumber
  });
  setEditDialog(true);
};
```

#### Удалено поле ввода из UI:
```tsx
{/* ❌ УДАЛЕНО */}
<div>
  <Label htmlFor="branchAccountID">{t('branch.account_id_label')}</Label>
  <Input
    id="branchAccountID"
    value={editData.accountID || ''}
    onChange={(e) => setEditData({ ...editData, accountID: e.target.value })}
    placeholder={t('branch.account_id_placeholder')}
  />
  <p className="text-xs text-muted-foreground mt-1">{t('branch.account_id_hint')}</p>
</div>
```

### 2. **src/pages/Settings.tsx** (без изменений)

Поле accountID остается на вкладке "Чат-бот":

```tsx
<Card className="border-none shadow-sm bg-white">
  <CardHeader>
    <CardTitle className="text-xl text-slate-800 flex items-center gap-2">
      🤖 Настройки бота
    </CardTitle>
  </CardHeader>
  <CardContent className="space-y-4">
    <div>
      <Label htmlFor="accountID">Account ID (WhatsApp Business API)</Label>
      <Input
        id="accountID"
        placeholder="Введите Account ID"
        value={botSettings.accountID}
        onChange={(e) => handleBotSettingsChange("accountID", e.target.value)}
      />
      <p className="text-xs text-slate-500 mt-1">
        Идентификатор аккаунта WhatsApp Business API для интеграции
      </p>
    </div>
    {/* ... другие настройки бота */}
  </CardContent>
</Card>
```

## Преимущества

1. **Единое место управления** - accountID теперь редактируется только в настройках бота
2. **Логическая группировка** - все настройки WhatsApp бота в одном месте:
   - accountID
   - managerTimeoutMinutes
   - systemPrompt
3. **Упрощение UI** - диалог редактирования филиала стал проще
4. **Меньше путаницы** - нет дублирования полей
5. **Безопасность** - меньше точек входа для изменения критичных данных

## Как использовать

### Для редактирования accountID:

1. Перейдите в **"Настройки"** (Settings)
2. Выберите вкладку **"🤖 Чат-бот"**
3. Найдите поле **"Account ID (WhatsApp Business API)"**
4. Введите или измените значение
5. Нажмите **"Сохранить настройки бота"**

### Для редактирования основной информации филиала:

1. Нажмите на название филиала в шапке
2. В открывшемся модальном окне нажмите ✏️ (карандаш) на нужном филиале
3. Доступны поля:
   - Название филиала
   - Адрес
   - Номер телефона
   - Фото филиала
4. Нажмите **"Сохранить"**

## Структура настроек бота

```
Страница Settings → Вкладка "Чат-бот"
├── 🤖 Настройки бота
│   ├── Account ID (WhatsApp Business API)
│   ├── Время молчания бота (в минутах)
│   └── [Кнопка] Сохранить настройки бота
└── 💬 Конструктор системного промпта
    ├── Системный промпт
    └── [Кнопка] Сохранить системный промпт
```

## Валидация

✅ TypeScript компиляция проходит успешно  
✅ Нет ошибок в консоли  
✅ UI корректно отображается  
✅ Функция обновления филиала работает без accountID  
✅ Настройки бота работают независимо  

## Связанные файлы

- `src/components/BranchSelector.tsx` - диалог редактирования филиала
- `src/pages/Settings.tsx` - страница настроек с accountID
- `src/contexts/BranchContext.tsx` - интерфейс Branch с полем accountID

## Заметки

- Поле `accountID` остается в интерфейсе `Branch` и в базе данных
- Backend API продолжает поддерживать обновление accountID
- Просто убрали возможность редактирования из диалога филиала
- Функциональность не сломана, только упрощен интерфейс
