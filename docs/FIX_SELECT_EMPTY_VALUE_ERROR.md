# Fix: SelectItem Empty Value Error

## 🐛 Проблема

При открытии диалога создания/редактирования задачи возникала ошибка:

```
ErrorBoundary caught an error: Error: A <Select.Item /> must have a value prop 
that is not an empty string. This is because the Select value can be set to an 
empty string to clear the selection and show the placeholder.
```

## 📍 Локация

**Файл**: `src/pages/Calendar/components/task-dialog-btn.tsx`  
**Строки**: 1388, 1393

## 🔍 Причина

В компоненте диалога задачи использовались `SelectItem` с пустым значением `value=""`:

```tsx
{!watchedServiceType && (
    <SelectItem value="" disabled>
        {t('task_dialog.select_service_for_durations')}
    </SelectItem>
)}
{watchedServiceType && availableDurations.length === 0 && (
    <SelectItem value="" disabled>
        {t('task_dialog.no_durations_available')}
    </SelectItem>
)}
```

**Почему это проблема:**

Radix UI Select не позволяет использовать пустую строку `""` как значение для `SelectItem`, даже если элемент отключен (`disabled`). Это связано с тем, что пустая строка используется для очистки выбора и показа placeholder.

## ✅ Решение

Заменены пустые значения на уникальные строковые идентификаторы:

```tsx
{!watchedServiceType && (
    <SelectItem value="__select_service_first__" disabled>
        {t('task_dialog.select_service_for_durations')}
    </SelectItem>
)}
{watchedServiceType && availableDurations.length === 0 && (
    <SelectItem value="__no_durations_available__" disabled>
        {t('task_dialog.no_durations_available')}
    </SelectItem>
)}
```

### Изменения:

1. **Первый SelectItem** (когда услуга не выбрана):
   - `value=""` → `value="__select_service_first__"`
   
2. **Второй SelectItem** (когда нет доступных длительностей):
   - `value=""` → `value="__no_durations_available__"`

## 🎯 Почему это работает

1. **Уникальные значения**: Используются специальные идентификаторы с двойным подчеркиванием, которые не конфликтуют с реальными значениями
2. **Disabled элементы**: Элементы остаются отключенными (`disabled`), поэтому пользователь не может их выбрать
3. **Соответствие Radix UI API**: Теперь код соответствует требованиям библиотеки

## 🧪 Тестирование

### Шаги воспроизведения (до исправления):
1. Открыть календарь
2. Нажать "Создать задачу" или открыть существующую задачу
3. **Ошибка**: ErrorBoundary перехватывает ошибку Select.Item

### Результат (после исправления):
1. Открыть календарь
2. Нажать "Создать задачу" или открыть существующую задачу
3. ✅ Диалог открывается без ошибок
4. ✅ Fallback опции отображаются правильно:
   - Когда услуга не выбрана: "Сначала выберите услугу для просмотра длительностей"
   - Когда нет доступных длительностей: "Нет доступных длительностей"

## 📊 Влияние

### Затронутые компоненты:
- ✅ Диалог создания задачи
- ✅ Диалог редактирования задачи
- ✅ Select длительности услуги

### Что исправлено:
- ✅ ErrorBoundary больше не перехватывает ошибку
- ✅ Диалог работает стабильно
- ✅ Fallback опции отображаются корректно

### Побочные эффекты:
- ❌ Нет - элементы остались `disabled`, их нельзя выбрать
- ❌ Нет изменений в UI/UX

## 📝 Best Practices для Radix UI Select

### ❌ Неправильно:
```tsx
<SelectItem value="">Placeholder</SelectItem>
<SelectItem value="" disabled>Disabled option</SelectItem>
```

### ✅ Правильно:

**Для placeholder:**
```tsx
<Select defaultValue="placeholder">
    <SelectTrigger>
        <SelectValue placeholder="Select option..." />
    </SelectTrigger>
    <SelectContent>
        <SelectItem value="option1">Option 1</SelectItem>
        <SelectItem value="option2">Option 2</SelectItem>
    </SelectContent>
</Select>
```

**Для disabled опций:**
```tsx
<SelectItem value="__disabled_option__" disabled>
    Cannot select this
</SelectItem>
```

**Для fallback сообщений:**
```tsx
{items.length === 0 && (
    <SelectItem value="__no_items__" disabled>
        No items available
    </SelectItem>
)}
```

## 🔗 Связанные ресурсы

- [Radix UI Select API](https://www.radix-ui.com/primitives/docs/components/select)
- [GitHub Issue: Empty string values](https://github.com/radix-ui/primitives/issues/1569)

## ✅ Статус

- **Исправлено**: ✅ Да
- **Протестировано**: ⏳ Требуется ручное тестирование
- **Дата**: 1 ноября 2025

---

**Автор**: GitHub Copilot  
**Версия**: 1.0.0
